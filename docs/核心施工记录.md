# 核心施工记录

> 每完成一个需求就做核心施工记录 + git 维护。记录：做了什么、为什么、实现了什么、取舍。

---

## 2026-08-31 · A22 会话持久化（T2）

**做了什么**：把 refresh token 从 kvstore（Redis/内存）迁移到持久化的 `user_sessions` 表。

**为什么**：原实现 refresh token 只存 kvstore——云端存 Redis、桌面本地模式存内存 kv。桌面端重启即掉登录，「3 端在线 + 踢最早」无落点，设备管理页无法列出设备（方案 v3 缺口 K5）。

**实现了什么**：
1. 新增 `user_sessions` 表（model/repo/repository 三件套），字段含 jti（唯一）、设备名/类型/IP、last_active_at、expires_at。
2. `issueTokens` 改写：签发时落一条 session 行（滑动 30 天窗口 = refresh TTL），并强制「单账号最多 3 会话」，超出踢最早活跃。
3. `Refresh` 改写：按 jti 原子消费旧 session（单条 DELETE，RowsAffected 判重，杜绝并发双刷），携带旧设备元数据签发新 session（避免刷新被误判成新设备撞 3 端上限）。
4. `Logout` 改写：单会话吊销走 `Consume(jti)`，全量下线走 `DeleteByUser`。
5. 新增 `GET /api/v1/auth/sessions`、`DELETE /api/v1/auth/sessions/:id` 设备管理端点；设备类型由 User-Agent 识别（Electron→桌面、mobile 关键词→移动、其余→web）。
6. 短信验证码仍走 kvstore（无需设备维度），不受影响。

**取舍**：保留 kvstore 仅用于短信验证码与限流；refresh 会话全量迁表。local 模式 SQLite 持久化，云端 PG 持久化，双模式行为一致。设备管理前端 UI 暂缓，后端端点已就绪。

**验收**：`go build ./... && go vet ./... && go test ./...` 全绿。

---

## 2026-08-31 · A23 阅读数据回流 + 追更计数修复

**做了什么**：① 作者侧新增阅读数据端点与面板；② 修复追更计数膨胀 bug。

**为什么**：飞轮「创作→发布→阅读→互动→反哺」缺最后一段——作者发布了作品却看不到任何读者数据（方案 v3 判断三：没有反馈闭环）。同时 `Follow` 无条件 `IncFollowCount`，重复点追更会让计数虚高。

**实现了什么**：
1. 新增 `GET /api/v1/publish/works/:wid/stats`，返回追更人数、累计阅读人数、每章当前阅读人数漏斗。
2. 仓储层新增 `ReadingDistribution`（按 chapter 聚合最新进度）与 `DistinctReaders`（COUNT DISTINCT user_id）。
3. 前端 `WorkStatsPanel` 组件 + 接入发布弹窗（PublishModal），展示追更/阅读指标卡与每章阅读漏斗条。
4. `Follow`/`Unfollow` 改为幂等：已关注则不重复计数、未关注则不重复扣减；`UpsertFollow` 冲突更新不再覆盖 `created_at`（原实现会把关注时间重置为零值）。

**取舍**：reading_progress 只存读者「最后位置」，每章人数 = 读者最后停留点，是对读完率的诚实近似；要精确到「每章完成率」需另加逐章进度事件表（列入后续 E5 埋点），本期不引入。

**验收**：`go build ./...`、`pnpm build` 全绿。

---

## 2026-08-31 · E5 交互式微创作（A28+A29）——产品护城河

**做了什么**：在公开阅读器上落地上层读者互动：段落情绪点击 + 划线评论 + 点赞 + 作者采纳。

**为什么**：这是《备忘录》idea 本体（「交互式微创作的小说阅读体验，低消耗的小说微调玩法」）与方案 v3 判断里唯一「抄不走」的护城河。E4 已把读者端建成，但仍是「只读孤岛」——没有互动就没有留存与作者反馈闭环。

**实现了什么**：
1. 数据模型：`interactions`（类型 comment/mood、block_index 锚定段落、payload 类型化、状态 pending/adopted/hidden、like_count）+ `interaction_votes`（user×interaction 唯一，点赞 toggle）。
2. 后端：`InteractionService` + `InteractionHandler`，端点 `GET/POST /read/chapters/:pid/interactions`、`POST /interactions/:iid/like`、`DELETE /interactions/:iid`、`POST /publish/interactions/:iid/adopt`。评论过内容安全（C12），采纳仅作者可操作（404 防探测）。
3. 前端：阅读器段落悬停浮出情绪条（燃/刀/甜/谜，一次点击即反馈）；选中文本浮出「划线评论」；右侧互动侧栏（情绪汇总、评论列表、点赞、作者采纳/隐藏、评论输入框）。

**取舍**：首期只做「划线评论 + 情绪点击 + 采纳」最小闭环（方案 Q4），分支投票/填空共创后置；「采纳」先落地为作者认可置态（已采纳徽标），自动填回正文的 `fill` 玩法待后续版本；读者被采纳后的 WS 站内通知待补（复用现有 WS Hub）。

**验收**：`go build ./...`、`go vet ./...`、`pnpm build` 全绿。

---

## 2026-08-31 · 修复：发布弹窗内容被遮挡（fixed 弹窗被困 containing block）

**现象**：文章发布窗口（PublishModal）打开后内容被遮挡/错位。

**根因**：代码库存在两套弹窗写法——`common/Modal.tsx`（portal 到 body，正确）与手写 `fixed inset-0` 弹窗（错误）。手写弹窗只要挂在带 `backdrop-filter`/`transform`/`filter` 的祖先节点下，该祖先就会成为 fixed 后代的 containing block，`inset-0` 不再覆盖视口，弹窗被压缩进祖先盒子里、内容被编辑器其它表面遮挡。PublishModal 恰好挂在 Toolbar 内，而 Toolbar 根节点带 `backdrop-blur`；`glass-panel`（common Modal 面板）同样带 `backdrop-filter: blur(16px)`，会殃及嵌套其中的手写 fixed 层（版本对比 DiffViewer）。

**修复**（全部改为 portal 到 `document.body`，与 common/Modal 策略一致）：
1. `PublishModal` —— 根节点包 `createPortal(document.body)`。
2. `DiffViewer` —— 根节点 portal（同时修复编辑器内与版本对比弹窗内两种使用场景）。
3. `VersionCompare` 加载遮罩 —— portal。

**为什么“总是”出这类问题**：手写 fixed 弹窗没有强制 portal 的约定，只要有人在带 backdrop-filter/transform 的容器里再挂一个手写弹窗就会复发。已在此三处注释中写明原因；后续新增弹窗应一律走 `common/Modal` 或自行 portal。

**验收**：`pnpm build` 全绿。

---

## 2026-08-31 · A31 章节情绪曲线（作者侧读者情绪反馈）

**做了什么**：把 E5 的段落情绪点击数据回传给作者——发布面板中每章可展开查看「情绪曲线」。

**为什么**：E5 收集了读者的燃/刀/甜/谜情绪，但作者看不到；这是飞轮「互动数据 → 反哺作者」的最后一公里，也是其他平台拿不到的细粒度反馈。

**实现了什么**：
1. 后端 `GET /api/v1/publish/chapters/:pid/emotions`（作者专属，404 防探测）：按 block_index 聚合情绪互动，返回每段四情绪计数 + 全章汇总。
2. `PublishService` 注入 `interactionRepo`，聚合逻辑在 service 层（JSON payload 解析 + 排序）。
3. 前端 `WorkStatsPanel` 章节行改为可展开：点击拉取情绪数据，SVG 折线图（四色：燃橙/刀红/甜粉/谜紫）+ 汇总 chips。

**取舍**：情绪曲线按「有情绪的 block」等距分布 X 轴（不做整章逐段铺开，避免无数据段拉平曲线）；仅作者可见。

**验收**：`go build/vet`、`pnpm build` 全绿。

---

## 2026-08-31 · A30 用量日聚合（T3）+ ai-service 依赖清单修复

**做了什么**：① 新增 `token_usage_daily` 日聚合表 + 写穿 + 查询端点；② 补齐 ai-service 缺失的依赖声明。

**为什么**：运营后台与用量面板要展示趋势，只能实时扫 append-only 的 `token_ledger`（数据量大后必然变慢）；且启动全部服务时发现 ai-service 的 `requirements.txt` 漏了 `httpx`/`pillow`（多 agent 施工遗留，缺其一无法启动，Docker 镜像同样会挂）。

**实现了什么**：
1. `token_usage_daily` 表（user_id+date 唯一），`text_units`/`image_count`/`image_units` 三计数。
2. `TokenService.Consume` 扣减成功后写穿日聚合（best-effort，失败仅 WARN 不阻断主链路）；`image_gen` 计入 image 计数，其余计 text。
3. 新增 `GET /api/v1/token/usage/daily?days=30`，读聚合表按日返回（比扫 ledger 快）。
4. `requirements.txt` 补 `httpx`、`pillow`。

**取舍**：日聚合按 `Asia/Shanghai` 时区切日（与现有 stats 一致）；`/token/stats` 仍读 ledger（其 day/week/month 三粒度与日表不同，切换属后续优化，待日表积累足够历史后再切）。写穿失败靠后续补跑可恢复，不引入定时回填任务。

**验收**：`go build/vet/test`、AI 服务 `python -m app.main` 启动、`/health` 返回 ok。

---

## 2026-08-31 · 启动全部服务（环境准备）

**做了什么**：本地拉起完整技术栈用于测试。

- 基础设施（Docker）：postgres:5432 / redis:6379 / nats:4222（已 running）。
- Go server：`:8080`（cloud 模式，22 个迁移已应用）。
- Web：`:3000`（vite dev，代理 /api→8080）。
- AI service：`:8100`（HTTP）+ `:50051`（gRPC），venv 用 `C:\Environments\python3.12.2` 创建（LobsterAI 运行时 python 无 venv 模块）。

**发现并修复**：ai-service `requirements.txt` 缺 `httpx`/`pillow`（见上）；LobsterAI 运行时 python 无 `venv`，改用系统 Python 3.12。

---

## 2026-08-31 · 社区发现页 + 阅读器追更（补齐产品全貌）

**做了什么**：补齐「全貌」缺口——新增公开发现页/社区首页 + 阅读器追更按钮。

**为什么**：项目有编辑器（创作端）、发布、阅读器、互动，但没有任何地方能「逛」公开作品——读者没有入口、飞轮没有起点、社区不存在。这是产品从「工具」变「平台」的临门一脚。

**实现了什么**：
1. 后端 `GET /api/v1/read/discover?q=&limit=&offset=`（匿名）：公开作品列表（标题/简介/封面/追更数/章节数/作者昵称/AI 标识/更新时间），按更新时间倒序，支持标题搜索（LOWER/LOWER 跨 PG/SQLite）。仓储层单条 JOIN+GROUP 查询，不 N+1。
2. 后端 `GET /api/v1/read/follows/:wid`：返回读者是否已追更（供阅读器初始化按钮状态）。
3. 前端 `DiscoverPage`（`/discover`）：氛围背景 + 搜索框 + 作品卡片网格（封面渐变占位/AI 徽标/追更数/章节数）+ 分页「加载更多」。匿名可访问，登录用户可回工作台。
4. 导航入口：TopBar 加「社区」链接（登录态）、LandingPage 加「浏览社区」按钮（游客）。
5. 阅读器顶栏加「追更/已追更」按钮（登录态显示，点击切换，乐观更新）。

**取舍**：发现页仅按更新时间倒序，未做推荐算法/分类（后续可加）；封面字段已存在但发布面板尚未提供封面上传入口（列为后续体验项）。

**验收**：`go build/vet`、`pnpm build` 全绿；`/read/discover` 实测返回作品数据。

---

## 2026-08-31 · Docker 镜像构建链路修复（dev 全栈 can't start）

**问题**：用户执行 `docker compose up -d` 失败，报 `corepack prepare pnpm@latest --activate` 网络错误，build web 镜像失败。

**根因（三处，逐一修复）**：
1. **corepack 网络解析**：`packages/web/Dockerfile`（含 `Dockerfile.dev`）用 `corepack prepare pnpm@latest`，需在容器内实时拉取 npmjs.org 解析 `@latest`，在受限网络下必失败。修复：改为**固定 pnpm 版本** `pnpm@11.13.0`（与本机一致），并新增 `ARG NPM_REGISTRY` 默认指向 `registry.npmmirror.com`，经 `npm_config_registry`/`COREPACK_NPM_REGISTRY` 注入，海外可在 build 时用 `--build-arg` 覆盖官方源。
2. **Node 版本过旧**：web 基础镜像 `node:20-alpine` 运行 pnpm@11 会报 `ERR_UNKNOWN_BUILTIN_MODULE`（pnpm 11 需 Node22+）。修复：升到 `node:22-alpine`。
3. **node_modules 被宿主覆盖**：web build context 为仓库根，`COPY packages/web/ packages/web/` 会把宿主 Windows 版 `node_modules` 拷进 Linux 容器，覆盖 pnpm 生成的符号链接，导致 `tsc` 无法解析。修复：新增根 `.dockerignore` 排除 `node_modules`、`.git`、`.env`、`packages/desktop` 等。

**连带修复（服务器/AI 镜像同类问题）**：
1. **Go 版本不匹配**：`packages/server/Dockerfile` 用 `golang:1.22-alpine`，但 `go.mod` 要求 `go >= 1.26.5`，`go mod download` 直接失败。修复：升到 `golang:1.26-alpine`。
2. **GOPROXY**：新增 `ARG GOPROXY` 默认 `goproxy.cn,direct`，解决模块下载受限。
3. **PIP 源**：`packages/ai-service/Dockerfile` 新增 `ARG PIP_INDEX` 默认清华镜像 `pypi.tuna.tsinghua.edu.cn/simple`，解决 pip 安装受限。

**取舍**：依赖源默认走中国镜像以贴合本机/国内网络，但全部做成 `ARG` 可覆盖，保证 CI/海外部署不变；`.dockerignore` 放在仓库根（web build context），server/ai 各自 context 不受影响。

**验收**：`docker build` 三镜像（web/server/ai-service）各自成功：web `naming to inkbloom-web:test done`、server `naming to inkbloom-server:test done`、ai `naming to inkbloom-ai:test done`。

---

## 2026-08-31 · 开发预制数据"变了"的调查结论（数据完整性核查）

**现象**：用户反馈"项目的开发预制数据变了"。

**调查过程与结论**：
1. **数据库数据完好**：PostgreSQL（named volume inkbloom_postgres_data）核查——37 张表、22 条 schema_migrations（001→024 全量应用）、8 个用户（demo 13800000000 等）、novels 10+、published_works 1 条（山雨欲来，3 章，1 追更）、Redis 75 个 refresh token key 均在。
2. **容器曾被重建**：inkbloom-postgres 的 RestartCount=0、StartedAt=2026-08-31T15:23Z（23:23 北京时间），即容器是**重新创建**而非重启——推测用户/其他 Agent 执行过 docker compose up -d（会重建应用容器，postgres 容器重建但 volume 保留，数据不丢）。
3. **"预制数据"的真身 = 前端 DEV_MOCK fallback**：packages/web/src/stores/novel-store.ts 中 DEV_MOCK = import.meta.env.DEV——**当后端 API 请求失败时，DEV 模式自动降级显示本地 mock 数据**（localNovels/localChapters/local-backend.ts）。因此当 server/postgres 重启的空窗期，页面会从"真实 DB 数据"切换到"本地 mock 演示数据"，用户感知为"预制数据变了"。
4. **另一个干扰源**：本机曾同时存在 eyoung-postgres（另一项目，占用 5432，3 分钟前 Exited），与 inkbloom-postgres 端口重叠过，也可能造成连接串扰。

**决策/取舍**：DEV_MOCK 降级机制本身是本地演示的合理设计（无后端也能跑通全链路），保留；但需向用户澄清这是**预期行为**而非数据丢失。未改代码。

**验收**：全链路重启后 discover API 返回真实数据（山雨欲来），前端 :3000 / 后端 :8080 / AI :8100 全部 HTTP 200。

---

## 2026-08-31 · P1 全本创作流水线——chapter 场景（Agent 接管创作第一步）

**背景**：用户要求把"玩具级"Agent 系统升级为可接管的创作工作流，对话内可选 AI 起稿、自动跑完全本创作。诊断发现 gents/team 是**为写代码设计的通用 TaskCard 模型**（affected_files/code_changes/验收标准），与小说领域脱节;且现有 scene 白名单缺"章节正文"场景，AI 无法产出可落库成稿。

**做了什么（P1 最小闭环：让 AI 能写"章"）**：
1. 设计文档：[施工简报-Agent创作工作流.md](file:///C:/Repository/Develop/aicoding/inkbloom/docs/process/施工简报-Agent创作工作流.md)——四层架构（发起层/流水线层/场景层/落库层）+ story_pipeline 状态机设计。
2. ai-service 新增 **chapter 场景**（pp/agents/scenes.py）：_chapter_user_prompt 以大纲结构/前文摘录/记忆为纲输出可落库正文；SCENES["chapter"] 配置（temperature 0.8、max_tokens 4096、two_step True）。
3. 契约扩展：pp/agents/models.py 的 AgentContext 增加可选 	arget_node（本章目标节点 title/summary/status）;scenes.py 的 _common_context 增加"本章目标"块。
4. Go 端对齐：service/agent_context.go 的 AgentContextData 增加 TargetNode（JSON tag 	arget_node），BuildAgentContext 根据 
odeID 匹配大纲节点并填充 target_node（title/summary/status）;handler/ai_handler.go 的 gentScenes 白名单加入 chapter，错误提示同步更新。

**取舍**：target_node 复用在 preceding_chapters 的 cutoff 逻辑（前文只带目标章之前），避免把已写章节全量喂给 LLM 控制 token;chapter 场景严格输出正文段落以供落库。

**验收**：go build 全绿;Python py_compile 通过;scene 注册表加载含 chapter;直接 POST AI /api/agents/generate（scene=chapter）返回 HTTP 200、scene:"chapter"——场景契约/上下文组装/Go↔Python 代理全通。唯一报错为 LLM api key 无效（环境未设 INKBLOOM_OPENAI_API_KEY，非代码问题）。

---

## 2026-09-01 · P1 全本创作流水线——story_job 状态机 + 端到端闭环打通

**背景**：在 chapter 场景（上一步）基础上，补齐真正的"全本创作流水线"：从一句话创意起稿，AI 按状态机自动推进（idea→outline→plan_chapters→draft_chapter→verify→finalize→done），逐章生成可落库正文并受控采纳。

**做了什么（P1 状态机 + 落库闭环）**：
1. **story_jobs 表**：model/story_job.go（stage/status/progress/total_steps/stage_payload/result/chapter_keys + 时间戳），GORM AutoMigrate 注册于 database.go;迁移  25_story_jobs.up.sql 加 (user_id,status)、(novel_id) 索引。
2. **仓储**：epository/story_job_repo.go（Create/GetByID/ListByNovel/ListByUser/Update/Delete，全按 user_id scope 隔离）。
3. **服务层**：service/story_service.go——核心状态机驱动：
   - CreateJob（从 idea + logline 建 job）
   - GenerateStage（按 stage 映射到 agent scene，经 gentContext.BuildAgentContext 组装上下文，调用 AI /api/agents/generate，产物存 stage_payload）
   - AdoptChapter（确认采纳，把正文写入真实 chapters 表，记录 adopted 数组）
   - AdvanceStage（状态机推进，末尾到 done 时写 result）
   - stageToScene（idea/outline→outline 场景，plan_chapters→outline，draft_chapter→chapter，其余→summary）
4. **DTO/Handler/路由**：dto/story_dto.go、handler/story_handler.go、http.go 注册 7 条 /api/v1/story/jobs* 路由（Create/List/Get/Delete/Generate/Adopt/Advance）。

**关键设计**：
- **受控落库**：AI 产物先存 stage_payload（预览态），作者调用 AdoptChapter 才写入真实 chapters——保留作者主导权，避免 AI 错误内容污染正式稿。
- **状态机可续跑**：job 持久化，断电/失败可恢复;每步进度、产物都落库。
- **复用而非新造**：上下文组装复用 AgentContextService，计费走现有 	oken_service，不重复造轮子。
- **幂等**：采纳记录进 stage_payload.adopted[]，用 chapter_key 去重。

**端到端实测（真实 DeepSeek LLM）**：
- 登录 demo → 创建 job(novel 33 剑试天下) → advance 到 draft_chapter → generate 生成 **3299 字高质量章节正文**（少年负剑出山、遇伏、血旗门伏笔，文笔出色）→ adopt 落库到 chapters(novel 33, id=41, 断剑出山) ✅
- 状态机：idea→outline→plan_chapters→draft_chapter→（最终 finalize→done）全链路可推进。
- 前置：配置 DeepSeek key(INKBLOOM_OPENAI_API_KEY)后 AI 服务真实生成。

**验收**：go build/go vet 全绿;story 路由注册;迁移 025 应用;端到端起稿→生成→采纳落库闭环实测通过。

---

## 2026-09-01 · 前端「AI 起稿」入口 + 全本创作工作流面板（A 核心业务前端落地）

**背景**：后端 story_job 状态机已就绪（上一步），补前端入口，让用户在对话内可触发「AI 起稿」并可视化跑完整条流水线。

**做了什么**：
1. **story-client.ts**：/story/jobs 全链路 API 封装（create/list/get/delete/generate/adopt/advance）+ 阶段中文标签。
2. **story-store.ts**：Zustand store 管理任务列表/激活任务/生成/采纳/推进/删除，含 loading/generating/adopting 状态。
3. **StoryWorkflowPanel.tsx**：全本创作工作流面板——创建表单（作品名+一句话创意）、阶段进度条（idea→outline→plan_chapters→draft_chapter→verify→finalize→done）、阶段产物预览（大纲/成稿正文，成稿支持展开全文）、「生成当前阶段」「下一阶段」「采纳到章节」按钮、任务列表与删除。
4. **入口集成**：
   - AIChatPanel 空状态新增「AI 起稿 · 全本创作」按钮（dispatch inkbloom:open-story-workflow）
   - RightPanel 新增 story 标签页（AI 起稿），监听事件切到该页
   - ui-store 的 RightTab 增加 story
   - agent-client 的 AgentScene 增加 chapter（供单章生成）

**关键交互**：
- **受控落库**：AI 生成的成稿正文在面板里先预览，作者点「采纳到章节」才写真实 chapters（与后端 AdoptChapter 呼应，保留作者主导权）。
- **可视化进度**：7 阶段进度条高亮当前阶段，章节数/进度实时回显。
- **续跑**：任务持久化，面板刷新后可继续推进/生成。

**验收**：pnpm build 全绿（2088 模块）;dev server :3000 HMR 生效;后端 story 路由/迁移/端到端已在上一轮实测通过。

---

## 2026-09-01 · 闭环创作 Agent（P2）——数据绑定 + 生成质量 + 参数化

**背景**：用户提 5 个缺陷，本质是"把 AI 从点状文本生成升级为闭环创作 Agent"。设计简报见 docs/process/施工简报-闭环创作Agent.md。

**做了什么（本轮 P2-a/b/c）**：
1. **生成质量（缺陷1）**：`scenes.py` chapter 场景 prompt 增加强制分段（空行分段）+ 插图占位符规范 `[插图：画面描述]`。
2. **读系统（缺陷5前半）**：`agent_context.go` 扩展 AgentContextService 注入 knowledgeRepo + foreshadowRepo（`WithKnowledgeAndForeshadow`），BuildAgentContext 新增 `knowledge_nodes` + `foreshadow_threads` 上下文，让 AI 生成时知道已有世界观实体与未回收伏笔；Python `models.py`/`scenes.py` 对应加字段 + 格式化块。
3. **写回系统（缺陷3/5核心，"化手动为自动"）**：`story_service.go` 注入 knowledgeService + foreshadowService（`WithClosedLoop`），`AdoptChapter` 采纳后自动 `ExtractFromChapter`（实体入知识图谱）+ `DetectPlants`（伏笔探测）。
4. **参数化（缺陷4前半）**：story_job 加 `config` JSONB（chapter_count/words_per_chapter/style/auto_settle），CreateJob 保存默认值，GenerateStage 把 config 注入生成 prompt；前端 `StoryWorkflowPanel` 加动态滑条（章节数/每章字数滑条 + 文风输入 + 自动沉淀开关）。
5. **orchestrator 空响应修复**：DeepSeek 高温偶发返回空 content（err=None），orchestrator 原来只在 err 时 fallback，导致空结果透传。修复：step2 空 content 也 fallback；one-shot 加重试（最多 3 次，逐次降温）。

**关键设计取舍**：
- 知识图谱是确定性抽取 → 采纳后直接入库（14 实体实测入图）；伏笔是概率性探测 → DetectPlants 只返回候选不自动入库（避免误报污染线索表），符合"伏笔需作者确认"的谨慎原则。
- 闭环绑定失败 best-effort，绝不阻断章节落库。

**端到端实测（真实 DeepSeek）**：
- 生成 2479 字成稿，含分段 + 插图占位符（概率性）。
- 采纳后自动沉淀：知识图谱 14 节点（临水城/断水山庄/沧浪剑谱/店小二等，类型正确），伏笔 5 候选。
- config 注入生效（8章/1500字/冷峻武侠回显）。

**验收**：`go build`/`go vet` 全绿；Python `py_compile` 过；端到端闭环实测通过。
