# 核心施工记录 — 2026-09-05

> 承接 2026-09-04（403 修复 `43f124f` 已记录于当日文件）。本篇覆盖自 403 修复
> 之后至本日内的全部核心施工，均未另行拆分记录，统一补记于此。

---

## 1. 批量发布只生效一章 — 根治（前端拦截器）

**现象**：发布管理多选勾选多章，实际只发布第一章，循环中断。

**根因**：`api-client.ts` 响应拦截器把业务码 `code !== 200` 一律判失败。而
`POST /publish/works/:wid/chapters` 返回 **201 Created** → 第一发即被前端
reject，循环中止；后端那一章实际已入库，造成"勾多章只发一章"。

**修复**：
- `api-client.ts`：非 2xx（`code < 200 || >= 300`）才算业务错误，201 正常解包。
- `PublishModal.handlePublishChapters`：逐章串行 + 单章失败不中断整批，
  汇总"成功 N 章 / 未推送 M 章"反馈，失败项保留勾选便于重试。

---

## 2. 发布版本唯一化

- `chapter_version_repo.go` 新增 `DeletePublishMilestones`（kind=milestone 且
  label 前缀「发布《」）。
- `PublishChapter` 写新发布快照前先清旧的：一章在历史面板只保留一条
  「发布《作品》」条目；重发=覆盖，不再堆积。
- 存量重复数据一次性清洗（DELETE 保留每章最新一条）。

---

## 3. 回收站整体退役

- 删除幕/要点改为**直接删除 + 确认弹窗**（提示"永久删除，无法恢复"），
  绑定章节一并删除；字数经 `RefreshNovelWordCount` 立即重算。
- 删除 `TrashModal.tsx` / `trash-store.ts` / `trash-client.ts` 及全部引用。

---

## 4. AI 成章入口 + 任务通知

- 抽出共享 hook `useChapterDraft`（scene=chapter，带大纲/前文/记忆/伏笔完整
  上下文的 Agent 管线），两处入口复用：
  - 节点编辑器「扩写成稿」（原有，迁移复用）；
  - 正文工具栏新增「AI 成章」按钮 → `inkbloom:ai-compose` 事件 →
    EditorArea 拉起预览，确认后覆盖当前编辑版本并落盘。
- 成章任务接入右侧栏任务通知：`task-store` 增加 `update` 动作与 `local`
  标记；成章是同步调用，包装为本地伪任务（type=`chapter_generate`）显示
  进行中→已完成/失败，终态自动销毁；本地任务不支持后端中止（隐藏按钮）。
- `TaskListPanel` 类型表新增 `chapter_generate: AI 成章`。

---

## 5. 新建章节行为

概览页「新建章节」：在**最新幕末尾**追加要点（无幕则先建幕），并立即打开
该要点的中央编辑 tab（要点编辑器承载标题/梗概/写正文/成章）。

---

## 6. 「第19章」存档名根治 + 版本管理三态（两轮迭代）

**根因实查**：发布 label 在发布时刻把当时的 `chapters.position+1` 烧进文本
（第79章发布时 position=18 → "第19章"，其后重排为 0）。数据/大纲绑定序与
position 实查一致，属纯文本脏数据。

**第一轮（label 修复）**：
- 发布 milestone label 改为只写 `发布《作品》`，**任何地方不再持久化章节
  序号**；展示序一律由前端按大纲实时计算。
- 存量 11 条脏 label 已剥离 `第N章` 后缀（SQL regexp_replace）。

**第二轮（版本三态落地，按用户最终裁决的两份模型）**：
- **三态定义**：
  - 草稿 = `chapters.content`（服务器第一份文章，工作区）；
  - 发布 = `published_chapters.content`（服务器第二份文章，冻结副本）+
    **快照指针** `version_id → chapter_versions` 发布 milestone；
  - 临时 = 浏览器 localStorage 暂存栈（`utils/temp-branch.ts`，每章独立、
    上限 20 条），服务器**永不保存**第三份。
- **曾按 git 原理实现三态（objects/commits/refs + 内容寻址去重），经验收
  判定为伪需求并整体回退**：migration 027、`chapter_git_repo`、backfill、
  `model.ChapterObject/Commit/Ref` 全部移除；发布/读者链路恢复整份副本。
- **新端点**（publish 域）：
  - `GET /chapters/:id/versions/summary` — 草稿/发布两态概要；
  - `GET /chapters/:id/versions/content?branch=published|draft` — 某态正文；
  - `POST /chapters/:id/versions/checkout/published` — 回滚=发布正文拷回
    草稿工作区（前端先自动暂存到临时分支，可撤销）。
- **前端**：
  - `HistoryPanel` 重写为**版本管理面板**：草稿卡 / 发布卡（对比、回滚）/
    临时卡（暂存栈：手动暂存、恢复、删除；恢复前自动再压栈保证可撤销）。
  - `DiffViewer` 重写为 **VSCode 级左右分屏对比**：行级 LCS + 修改行配对、
    行内 token 二级高亮、双侧行号、增删行红绿底、补位行灰底、单一滚动容器
    同步滚动、`N 处变更` 徽标；**portal 到 body + 默认 z-[1300]**，根治
    "对比被窗口遮住"（不依赖宿主层级）。
  - `VersionCompare`（共享组件）：草稿 vs 发布对比 + 回滚（先压临时分支）；
    OutlinePanel 小飞机入口与 PublishModal 对比按钮统一改走此组件。
  - `history-store.ts`（旧版本历史 store）删除；AI 改写前存点改为压入
    浏览器临时分支（TipTapEditor）。

---

## 7. 大纲列表与节点编辑器 UI 调整

- 大纲列表节点卡：**移除写作状态两态切换 chip**；小飞机（Send）图标统一
  承担发布/对比/回滚入口（灰=未发布，亮=已发布）。
- **发布态与写作/完成态解耦**：发布只写「已完成」标记（不再写 `published`
  状态、不再锁定状态位）；取消发布不回改状态；旧 `published` 数据按已完成
  展示（Toolbar / 要点编辑器 / 列表圆点一致）。
- 要点编辑器：扩写成稿、写正文、状态标记、上/下移、删除、保存**全部合并到
  标题正下方**的操作行（`flex items-center gap-1.5 pb-2 flex-wrap`），
  原底栏与独立状态行删除。

---

## 验证汇总

- `go build ./...` 通过；`go test ./internal/service ./internal/repository`
  通过（SnapshotForAgent 测试随三态迭代同步修正后回退保持原语义）。
- `tsc --noEmit` 通过。
- go server 多次 `-Build` 式重建重启（8080 healthy）；数据实查：发布
  milestone 每章唯一、label 无烧死序号、published_chapters 保留完整第二份
  正文 + version_id 指针。
- 遗留提醒：IDE 打开的旧 buffer 多次回写在途编辑（PublishModal /
  OutlinePanel / HistoryPanel 均复现），改文件前请先关闭旧标签页；前端改动
  需 Ctrl+Shift+R 强刷。

---

## 8. 版本管理二次打磨（工作区模型 + 回滚覆盖根治）

### 需求裁决（用户逐条指令 + 三项确认）

- 发布正文**只在请求时拉取**：summary 常驻仅元数据，对比/回滚才传正文（上轮已满足，保持）。
- **所有快照都可点击对比**：工作区快照对比 = 快照 vs 当前正文，纯浏览器渲染零服务器流量。
- 「回滚到发布未稳定覆盖草稿」**根因修复**：`CheckoutPublishedVersion` 此前未失效
  章节正文缓存（`cache.ChapterContent`），回滚后 3 分钟内 `GET /content` 命中旧缓存，
  前端把旧草稿灌回编辑器。修复：PublishService 注入 `cache.CacheManager` 并在
  checkout 时失效；前端改为**直接用 checkout 响应体灌 tab**（不再二次 GET）。
- **临时 + 草稿栏合并为「工作区」**（置于发布栏下方）：浏览器本地存储，
  自动快照 ×1 + 手动快照 ×2 固定槽位；`temp-branch.ts` 重写为
  `putAutoSnapshot / putManualSnapshot / replaceManualSnapshot` 槽位模型
  （localStorage key 迁移为 `inkbloom-workspace-*`，旧临时栈数据作废）。
- **手动槽超限**：不自动覆盖——面板内嵌替换选择器，装载两条手动快照标签元素，
  点击选中高亮，「确定替换」写入选中槽。
- **自动快照时机**（putAutoSnapshot）：回滚到发布版前、AI 全篇/局部改写覆盖前
  （含 AI 成章与扩写覆盖）、点击「已完成」时（Toolbar 两态切换到 done）。
- **草稿浏览器快存 + 服务器降频**（备忘录 L29）：本地快存保持 1s（draft-vault），
  服务器自动保存防抖 2s → **15s**；切章 / 关标签页 / 组件卸载 /
  `visibilitychange hidden` / `blur` 立即 flush。丢失窗口 = 15s，有浏览器快存兜底。

### 验证

- `go build` + `go test ./internal/service` 通过；`tsc --noEmit` 通过。
- go server 重建重启（NATS filtered consumer 冲突复现一次，`docker restart
  inkbloom-nats` 后恢复，与既往处理一致）。

---

## 9. 全局 Ctrl+S 手动保存兜底

- 浏览器默认 Ctrl+S 是"保存网页 HTML"（对 SPA 毫无意义且易误触）。在
  EditorArea 以 **capture 阶段** window keydown 拦截 Ctrl+S / Cmd+S
  （排除 Alt/Shift 组合），preventDefault + stopPropagation。
- 映射为「保存当前激活章节 tab」：复用防抖 flush 链路（清计时器 + 脏则
  立即 saveChapter），保存状态由底部状态栏展示；无激活章节 tab 时仅吞键。
  与 15s 服务器降频配合，构成"浏览器快存 + 降频 + 手动兜底"三级保存。

---

## 10. 统一 AIGC 配置卡片 + 右侧板迁移（备忘录 L61）

### 统一 AIGC 配置卡片（新组件 `components/ai/AigcCard.tsx`）

- 全项目 AI 生成交互统一为「AIGC」配置卡片：按钮名一律 **AIGC**；点击弹出
  与右侧板同风格的**附加指令对话框**（无技能选择；输入**可选**，拼接为
  「附加要求：…」；可直接确认，确认后对话框收起）。
- **多态调用**：`scene` 决定 Agent 工作流（character/setting/summary/
  inspiration/outline/chapter），`mode='image'` 走既有立绘图片任务链路
  （/aigc/prompt → /aigc/generate → 轮询）。
- **可取消 + 任务通知**：文本工作流 AbortController 真中断；运行中注册
  本地伪任务（type=`aigc_<scene>`，local:true）推送右侧任务栏，终态自动销毁。
- **宿主管线模式**（`onGenerate` + 受控 `running`）：成章等自带预览确认/
  任务登记的工作流传 instruction 给宿主，卡片不重复登记任务。
- 记忆库 AIGC 可见性（ai_visible / 章节锁）由服务端 agent 上下文过滤始终
  优先，卡片不绕过。
- agent-client.ts：`agentGenerate` 增加 `signal` 参数支持中断。

### 各场景接入（多态落地）

- **正文编辑**（EditorArea）：删除工具栏「AI 成章」按钮；编辑器顶部统一
  AIGC 卡（scene=chapter，宿主管线）→ useChapterDraft 成章管线 →
  DraftPreviewModal 预览确认后覆盖（覆盖前自动快照不变）。
- **记忆条目四 Tab 各带 AIGC 卡**（MemoryEditorModal）：
  基本资料（scene=分组，产物按「字段名：内容」逐行回填表单，识别失败退入
  详情预览；上下文携带详情正文摘录——表单 ↔ 详情相互拉取）、
  人物关系（产物按「目标 | 关系 | 备注」逐行追加）、
  详情（aigcSlot 卡，产物进既有预览弹窗插入光标）、
  立绘（mode=image，基于「外貌特征」拼 Prompt，产物追加图片墙）。
- **大纲节点编辑器**（OutlineNodeEditor）：要点编辑器顶部 AIGC 卡
  （scene=outline），产物转编辑器 HTML 覆盖梗概草稿（点「保存」才落库）。
- **随记**（MemoPad）与**自媒体**（MediaEditorArea）：编辑器顶部 AIGC 卡
  （scene=inspiration / summary），产物转 HTML 经 insert-content 定向插入
  光标处（编辑器新增 `insertTarget`）。
- **插槽统一**：TipTapEditor 新增 `aigcSlot`（渲染在工具列表最上方），
  所有富文本编辑器统一；移除旧 `onAIGC/aigcLoading` Sparkles 按钮链路。
- 修复 insert-content 对 plain 编辑器（记忆详情）把**定向投递**也丢弃的
  既有 bug：现在仅屏蔽无目标的全局广播，target 精确命中放行。

### 右侧板迁移

- 节奏 / 仪表盘 / 灵感包迁入右侧板新「**洞察**」Tab
  （`InsightPanel.tsx`，三子视图切换；`insightView` 入 ui-store 供命令
  面板深链）；批注本就在右侧板（批注评审 Tab）。
- 正文工具栏对应按钮此前已清理；命令面板三项改为洞察 Tab 深链
  （随记模式无右侧板，保留仪表盘/灵感包弹窗）；RhythmModal.tsx 删除，
  ui-store 移除 rhythmOpen/setRhythmOpen。

### 全本版本迁移

- 整本里程碑快照入口迁至**全书概览页**操作区（「全本版本」按钮 →
  novelVersionOpen），为后续世界线导出做准备；NovelVersionPanel 面板本体
  不变（全局抽屉）。

### 验证

- `tsc --noEmit` 通过；Vite HMR 全量生效无报错。纯前端改动，后端无需重建。

---

## 11. AI 记忆访问闸门：3 软闸 + 3 硬闸（备忘录规格落地）

### 需求

记忆条目新增六种 AI 访问权限（编辑器内单选，默认全部关闭=不限制）：

| 闸门 | 模式 | 语义 |
| --- | --- | --- |
| 软闸 | `ignore` AI全局忽略 | 注入但要求忽略，除非人类指令明确提及 |
| 软闸 | `restricted_visible` AI限制可见 | 单选解锁章，该章及以后可见；此前视为不可见，绝对不能剧透/提前显露，允许伏笔或隐晦线索 |
| 软闸 | `partial_visible` AI局部可见 | 多选可见章，选中章可见、其余不可见；同样禁剧透、允许伏笔 |
| 硬闸 | `disabled` AI全局禁用 | 绝不向 AI 注入该条目 |
| 硬闸 | `restricted_disabled` AI限制禁用 | 单选解锁章，该章及以后才注入 |
| 硬闸 | `partial_disabled` AI局部禁用 | 多选注入章，仅选中章注入 |

### 服务端求值（Go，唯一权威）

- `agent_context.go`：新增 `agentMemoryAccess`（mode + unlock_chapter_id +
  visible_chapter_ids，引用大纲节点 id）与 `resolveMemoryItems`——按写作位置
  （targetNodeID 在大纲 acts→nodes 展开序中的位次）求值：
  - 硬闸位置不符**直接丢弃**，绝不进 payload（唯一绝对保证）；
  - 软闸照常注入，附 `access` 指令（`ignore`/`hidden` + Go 渲染的中文约束
    说明，Python 只负责打印）；
  - **fail-closed**：无写作位置（大纲生成/纯聊天）或闸门参数缺失（未选解锁
    章/可见章集合为空）时，硬闸不注入、软闸按 hidden 处理——宁可保守不可剧透；
  - 旧字段迁移（`effectiveAccess`）：`ai_visible=false` → disabled；
    非空 `visible_chapters` → partial_visible；已有 `ai_access` 原样优先。
- 集中规则块：存在任一软闸条目时 `memory_access_rules` 携带「记忆访问规则」
  汇总文本，Python 渲染为独立 prompt 块。
- `target_item` 显式引用语义：AIGC 卡点名的条目不受闸门约束（闸门只管记忆
  列表的被动注入）——显式点名等同「人类指令明确提及」。
- `agent_service.go`：`write_chapter` 新增 `outlineNodeIDForChapter`，把章节
  绑定的大纲节点作为写作位置传入上下文组装（备忘录 L59：章节=大纲要点）；
  未绑大纲的章节传 nil，位置型闸门按保守策略处理。
- 大纲节点输出携带 `chapter_id`（0=未成稿），Agent 按大纲序认章节。

### Python 契约与渲染

- `models.py`：`MemoryItemAccessCtx`（visibility ignore/hidden + note）、
  `AgentContextData.memory_access_rules`。
- `scenes.py`：条目级标注 `［访问限制·忽略/隐藏］` + `限制说明：{note}` 行；
  `_common_context` 在前部渲染集中「记忆访问规则」块。

### 前端（展示侧镜像求值 + 编辑 UI）

- `memory-store.ts`：`MemoryAccessMode`/`MemoryAccess` 类型；
  `normalizeAccess` 读取时幂等迁移旧字段（旧字段不再生效、提交不再写入）。
- 新增 `utils/memory-access.ts`：与服务端同一套规则的**展示侧镜像求值**
  （`buildAccessEvalContext`/`evaluateAccess`/`describeAccess`），两处语义
  必须同步修改。
- 注入点接入：
  - `useChapterDraft`（AI 成章）注入设定清单按镜像求值过滤（按 nodeId 位置）；
  - `StoryWorkflowPanel`（全本创作线索库）与 `useAigcClues`（AIGC 卡摘录）
    创意阶段无写作位置，位置型硬闸 fail-closed 不进线索；
  - `MemoryEditorModal`：「AI 访问权限」六模式单选面板（软闸/硬闸分组、
    restricted_* 单选解锁章、partial_* 多选章节集合、失效节点 id 警示与
    一键清理、提交过滤失效 id）；
  - `MemoryPanel` / `MemoryExpandedView`：列表行与卡片六模式徽标
    （无限制不显示）。

### 验证

- `go build ./...` 通过；`go test ./internal/service/` 相关用例通过——
  `agent_context_test.go` 补齐 `BuildAgentContext` 新签名（opts 参数），并新增
  `TestResolveMemoryItemsSixGates`（21 子用例：六模式×位置内外、无写作位置
  fail-closed、参数缺失 fail-closed、旧字段迁移、ai_access 优先、未知模式
  容错）与 `TestMemoryAccessRules`。
- `tsc --noEmit` 通过（修复 `useAigcClues.ts` inflight 类型标注
  `Promise<void>` → `Promise<AigcClueExcerpts>`）。
- `py_compile` models.py / scenes.py 通过；test_scenes.py 无旧格式引用。
- 服务端为唯一权威求值方；前端镜像仅影响展示清单，不构成防剧透保证。

---

## 12. AIGC 卡可选上下文注入统一 + 全本AIGC 换统一卡 + 左侧板改版（作品集成条 / 架构栏目）

### AIGC 卡可选上下文注入（！项根治：所有卡片统一）

- **AigcCard 新增上下文勾选行**（架构 / 大纲 / 记忆 / 悬念 / 本章正文 /
  附近章节）：**默认勾选架构、大纲、记忆**，悬念与章节类可选；无内容来源时
  对应 chip 禁用（提示「暂无XX内容 / 线索加载中」）。立绘（image）与无作品
  上下文（随记 / 自媒体）自动隐藏该行。
- **分工**（复查结论：上下文装配的真实链路在服务端 AgentContextService）：
  - 大纲 / 记忆 / 悬念 → **服务端开关**：`AgentGenerateRequest.context`
    （outline/memory/foreshadow）→ `BuildAgentContext(..., opts)` 条件装配
    （dto.AgentContextFlags，nil = 全量兼容旧调用方）；记忆门控（六模式）
    仍由服务端求值，**可见性优先不被绕过**；TargetItem 显式点名不受
    memory 开关约束。
  - 架构 / 本章正文 / 附近章节 → **客户端摘录**（服务端无此数据）：确认时
    以「【参考上下文】」块拼接进指令（架构 800 字、本章 1200、附近各 400）。
- **线索摘录统一加载**（hooks/useAigcClues.ts）：架构（architecture-store）+
  大纲 / 记忆（六门控镜像 evaluateAccess 求值，与 useChapterDraft 同规则）+
  悬念（planted/reminded），60s TTL 缓存；本章 / 附近从 novel-store 现取。
- **成章管线透传**：useChapterDraft.generate 增加 `context` 开关；EditorArea
  成章卡 onGenerate(instruction, ctx) 把勾选开关传给 generateChapter——
  取消勾选大纲/记忆在成章链路同样生效。
- 后端：`BuildAgentContext` 新增 `AgentContextOptions`（story_service /
  agent_service 内部调用传 nil = 全量）；go build + 相关测试通过。

### 全本AIGC 页统一 AIGC 卡（！项）

- StoryWorkflowPanel：移除内联「线索库勾选卡 + AIGC 全概览」按钮，改用
  统一 AigcCard（宿主管线，scene=summary，running=fillingAll）——附加指令
  对话框 / 取消 / 任务通知 / 上下文勾选与其他卡片完全同形态。
- 卡内勾选快照（ctx）→ StoryOverviewClue[]（架构/大纲/记忆/伏笔）传
  generateStoryOverview；ai-service `clue_labels` 增加 `"architecture": "架构"`
  （未知 kind 原本回退英文标签）。
- 单项 ✦ 字段生成改用 cluesRef（架构/大纲/记忆/伏笔全量非空摘录，切换作品
  时加载，记忆仍走六门控镜像过滤）。

### 左侧板改版

- **作品集成标签条**（新 WorksBar，置于左侧板选项上方，仅小说作者）：
  作品库折叠开关在左，点击向下展开作品库列表（原窗口规格：图标 + 标题 +
  字数，可滑动，保留展开大视图入口）；**下展列表首个列元素 = 「创建小说」
  按钮**（与作品行同尺寸、同配色方案，明确显示文字），点击**悬浮式下拉**
  展开创建表单（标题输入 + 创建 / 创建并开启全本创作；交互与作品库下展
  一致：点击开、再点收；原 Modal 弹窗移除）；右侧 = **仅显示当前作品项**
  （作品库选中态同款样式：渐变底 + 左侧指示条 + 品牌图标；标签形态、
  **取消字数显示**），点击跳转概览页。
  NovelList.tsx 删除（作品库 Tab 由 WorksBar 取代）。
- **小说架构栏目**（大纲右侧新 Tab）：
  - `stores/architecture-store.ts`（参考记忆库结构预制）：基本信息
    （流派 / 主题 / 标签 / 规划章节数 / 规划每章字数 / 发布渠道，首次以
    作品流派预填）+ 核心线索 / 角色动力学 / 世界构建 / 情节构建四组类
    （条目 = 标题 + 内容，增删改）；持久化 localStorage
    （inkbloom-architecture-<novelId>，预制阶段先落地，后续再接服务端）。
  - `ArchitecturePanel.tsx`：基本信息表单 + 四组类折叠区块；
    `architectureText()` 序列化作为 AIGC 卡「架构」线索源（默认勾选）。
  - ui-store LeftTab 增加 'architecture'；知识图谱面板随大纲 Tab 展示。

### 追加修订（按用户反馈迭代）

- **WorksBar 配色**：作品库开关默认即品牌预设激活色（brand/15），展开时更亮
  （brand/35 + 辉光）；右侧当前作品项 hover 更亮（brand/35 + 边框提亮）。
- **创建小说交互**：移除悬浮下拉表单——点击「创建小说」直接在中央区域打开
  AI 起稿页（deselectNovel + centerTab=story）；AI 起稿页右上角新增关闭 X
  （返回概览）；小说未创建时底部按钮动态显示书名（创建「书名」并开启全本创作）。
- **概览页布局**：AI 起稿按钮移至右侧、置于「全本版本」左侧。
- **容器缩放**：当前作品项 flex-1 min-w-0 + truncate，随面板宽度伸缩、
  文本按空间增减；作品库 chip shrink-0。
- **bug 修复**：OutlineNodeEditor 在大纲未加载时误报「要点不存在或已删除」
  ——未加载先自动 loadOutline 并显示加载态，加载完成后仍无此要点才判定
  已删除并关闭 tab。

### 大纲面板节点卡改版（第二轮反馈）

- **发布标签纯标识**：小飞机不再可点击（移除 发布版vs编辑版 对比入口与
  VersionCompare 引用），仅展示已发布（sky 点亮）/未发布（灰）；发布操作在
  概览页、版本对比/回滚在章节版本历史面板。
- **移除节点简介预览**：卡片不再渲染 summary（梗概在要点编辑器内查看）。
- **省略号菜单**（发布标右侧 MoreHorizontal，点击外部关闭）：在上方添加 /
  在下方添加（outline-store 新增 `addNodeAt` 幕内按位插入）/ 章节简述AIGC
  （打开要点编辑器，顶部即剧情要点 AIGC 卡）/ 章节正文AIGC（有绑定章节 →
  打开正文并派发 inkbloom:ai-compose 成章管线；无绑定 → 打开要点编辑器用
  扩写成稿）/ 导出章节（节点信息 + 正文 → 下载 .md）/ 删除章节（确认后
  删节点 + 绑定章节）。
- **顺序标签**：节点标题头与幕标题头各含顺序标签（绑定大纲次序，跨幕连续
  计数，不写入标题文本），点击循环三种渲染：第一章（中文，utils/cn-numeral
  .ts）/ 1（数字）/ 隐藏；模式存 ui-store.outlineNumMode（持久化），
  节点「章」/幕「幕」后缀共享同一模式。

### 验证

- `go build ./...` + `go test -run TestBuildAgentContext` 通过；
  `tsc --noEmit` 通过；`start-dev.ps1 -Stop + -Build` 重启后 go/ai 双服务
  healthy（context 开关与架构线索 label 需新二进制 / ai-service 才生效）。
- 兼容性：旧前端 + 新后端（不传 context）= 全量注入，行为不变。

## 13. 大纲面板 UI/UX 修缮：顶层省略号菜单 / 顺序标签规格 / 自制确认弹窗 / 展开视图入口

### 背景

上一轮（第 12 节）落地的大纲省略号菜单存在层级 bug：菜单用 absolute 定位挂在
滚动容器（overflow-y-auto）内部，节点靠近面板底部时被裁剪遮挡。此外原生
window.confirm 散布 11 处观感差；顺序标签需衬线字体与幕级加粗规格区分。

### 做了什么

- **省略号菜单顶层化（bug 修复）**：`OutlinePanel.tsx` NodeCard 菜单改为
  `createPortal` 到 body + fixed 定位（按按钮 getBoundingClientRect 计算，
  底部空间不足自动向上翻转、左边界钳制，z-index 1040/1041），不再被滚动
  容器裁剪；菜单项保持：上/下方添加、章节简述AIGC、章节正文AIGC、导出章节、删除章节。
- **顺序标签规格**：OrderTag 采用 `.font-display`（Noto Serif SC 衬线，与右侧
  正文文本区分）；新增 `variant`：幕标题头 `act`（text-[11px] font-bold）、
  章节点 `node`（text-[10px] font-semibold）。渲染模式（第一章/1/隐藏）仍只存
  浏览器（zustand persist → localStorage），不入服务器。
- **自制确认弹窗**：新增 `components/common/ConfirmDialog.tsx`——Promise 风格
  `confirmDialog({title, message, confirmText, danger})`，复用全局 Modal
  （portal + 弹窗栈 Esc/遮罩语义），危险操作红色确认钮。全仓 11 处
  window.confirm 全部替换：OutlinePanel（删章节/删幕）、OutlineNodeEditor、
  WorldTreePanel、DraftResultCard、TaskListPanel、PublishModal（重发布/取消发布）、
  StoryWorkflowPanel（单字段覆盖/全概览覆盖/删任务）。
- **展开大视图入口**：WorksBar 作品库下展列表的「展开大视图」（px-3 pt-1 pb-1.5）
  从列表底部移至列表上方顶部并右置。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）；`grep window.confirm` 仅剩
  ConfirmDialog.tsx 注释 2 处，调用点 0 残留。

### 验收

- 大纲任意位置的省略号菜单完整可见（含面板底部节点）。
- 删除章节/幕、取消发布、中止任务等确认均为自制弹窗，Esc/遮罩可关。
- 幕/章顺序标签衬线字体渲染，幕标签加粗大字号；三态切换仅浏览器持久化。

## 14. 全书首页 / AI 起稿改为可关闭的多标签 Home 页

### 背景

此前「全书首页（作品概览）」与「AI 起稿」是中央编辑区在无章节 tab 时的兜底视图
（`ui-store.centerTab` 控制），并非标签。WorksBar 点击当前书只是把 `centerTab`
置为 `overview`，若已有章节 tab 激活则毫无反应——「点击当前书」失去意义。
用户要求：全书首页做成多标签栏里可关闭的 Home 页，点击当前书才真正有跳转语义。

### 做了什么

- **tab-store 扩展 Home 类 tab**：`TabKind` 新增 `'overview' | 'story'`；导出
  `overviewTabKey(novelId)`（每部作品一个首页 tab）与 `STORY_TAB_KEY`（AI 起稿全局单例）。
  复用 `openPanelTab` 打开，天然进入既有标签栏，支持点击切换 / ×/中键关闭。
- **EditorArea 渲染 Home tab**：无 tab 时仅显示欢迎页，移除 `centerTab` 分支；
  panel 渲染区新增 `overview`（`<NovelOverview novelId>`）与 `story`
  （`<StoryWorkflowPanel tabKey>`）两类常驻挂载、hidden 切换。标签栏为 Home 类
  tab 加图标区分（首页 Home 品牌色 / 起稿 Wand2 紫色）。
- **激活联动选书**：新增 `syncNovelForOverview`——切换/关闭后露出 overview tab 时，
  若其 novelId 与 currentNovel 不一致则先 `selectNovel` 再 `setActive`，保证
  「点击当前书 → 聚焦其首页」成立。
- **入口改造**：WorksBar / 命令面板选书 → `openPanelTab(overviewTabKey)`；
  `inkbloom:open-story-workflow` 事件 → 打开 story Home tab；StoryWorkflowPanel
  「返回作品概览」= 开当前书首页 + 关自身，右上角 X = 直接关 tab。
- **NovelOverview 去 currentNovel 依赖**：改为按 `novelId` prop 从 novels 取书，
  支持多作品首页并存。
- **生命周期**：删除作品时 `deleteNovel` 统一 `closeTab(overviewTabKey(id))`；
  刷新恢复上次作品后自动重开其首页 tab（保留原「刷新回概览」体验）。
- **清理**：移除 `ui-store` 的 `centerTab` / `setCenterTab`（已无消费者）。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。

### 验收

- 中央标签栏出现「全书首页」「AI 起稿」两类可关闭 tab，×/中键均可关。
- WorksBar 点击当前书 → 首页 tab 激活并置前；已有章节 tab 时同样生效。
- 打开另一部书的首页 tab → 自动切换 currentNovel 到该书。
- 删除作品 → 其首页 tab 自动关闭；刷新 → 自动恢复上次作品并打开其首页。
## 15. 顶栏布局调整：创作场景右移 / 社区左移 / 搜索框缩短

### 做了什么

- `TopBar.tsx` 三段式重排：
  - 左区 = Logo + AI 标 + 社区入口（社区从原中区左侧独立项移入，紧随 AI 标右侧）。
  - 中区 = 居中搜索，宽度缩短 1/4：`w-[min(480px,38vw)]` → `w-[min(360px,28.5vw)]`。
  - 右区 = 创作场景切换（RoleSwitcher，从 Logo 右侧移至最大化按钮左边）+ 最大化 + UserMenu。
- 左右两区仍 `flex-1` 等宽，搜索保持真正居中。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。
## 14. 顺序标签三态修正：第三态由「隐藏」改为「不含文字的节点标」

### 背景

第 12/13 节把顺序标签第三态实现成了 hidden（点击后标签整体消失），与需求
「第一章 / 1 / 不含文字的节点标」不符；且旧 localStorage 持久化的 'hidden'
会让标签一直不可见（被误读为"顺序标签被隐藏了"）。

### 做了什么

- `ui-store.ts`：`OutlineNumMode` 第三态改为 `'blank'`（不含文字的节点标）；
  persist 升 version 2 + migrate（旧值 'hidden' → 'blank'）；循环 cn → num →
  blank → cn；默认仍为 'cn'（中文）。
- `OutlinePanel.tsx` OrderTag：blank 态渲染无文字小标记（章 10px / 幕 12px
  方块，保留品牌/幕紫色调边框底色，可继续点击切换），并带 aria-label 兜底；
  组件内再兜底 'hidden' → 'blank'（双保险）。font-display 衬线与幕加粗变体
  （第 13 节）保留。

### 端到端实测

- `tsc --noEmit` exit 0；`grep -n hidden packages/web/src/stores/ui-store.ts`
  仅剩 migrate 注释。

### 验收

- 点击顺序标签在 第一章 → 1 → 无文字小标记 三态间循环，默认中文；
  旧浏览器存储不会导致标签消失。

## 16. 大纲节点卡：写作状态点移入正文标

### 做了什么

- `OutlinePanel.tsx` NodeCard：章名行前的 `w-1.5 h-1.5` 状态圆点移除，
  改放正文标（`正文/写正文` 按钮）内部右靠（文本之后），绿=已完成、黄=写作中，
  仍走 `STATUS_DOT[writingStatus]` 两态口径（published 归并为 done 展示）。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。
## 17. 写作状态二合一 + 节点页发布控制 + 连续发布规则收紧

### 背景

用户指令：「写作中 已完成 合一 绑定黄绿点」——确认后拆为三点：① 大纲列表
状态点保持仅显示；② 节点页写作状态切换二合一（无提示）；③ 节点页右侧新增
发布/未发布二合一控制（带确认弹窗），发布管理同步收紧为连续发布规则。

### 做了什么

- `OutlineNodeEditor.tsx`：
  - 原「写作中 / 已完成」两枚并排按钮合并为一枚二合一按钮：黄/绿点 +
    当前态文案，点击即切 `toggleWritingStatus`（无确认提示）；published
    态归并按已完成展示。
  - 操作行右侧（字数序号前）新增「未发布/已发布」二合一控制（sky 色系圆点），
    点击弹 `confirmDialog` 确认后走 `publishChapter` / `unpublishChapter`
    + `publish-store` 同步；发布成功将节点写作态写为 done（发布态解耦口径）。
    禁用原因逐一提示：未绑定正文 / 作品未发布 / 前序未连续发布 / 后序仍在发布。
- `PublishModal.tsx`（发布管理规则收紧）：
  - 发布规则改为：**第 n 章可发布 ⟺ 大纲序 1..n-1 章已全部发布**。
    发布时用「实时已发布集合」逐章推进（publishedLive），取代旧的
    「未绑定章节即断线」前缀判定；失败项报「不在连续前缀内」。
  - 章节列表勾选框对前沿之后的章节禁用（tooltip 说明须连续发布）；
    「全选未发布」只勾选前沿内可发布章节（selectableChapters）。
  - 取消发布必须从最后一章起：其后仍有已发布章节时按钮禁用并阻断提示。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。

### 验收

- 节点页：单按钮在 写作中↔已完成 间免确认切换；右侧发布控制按连续规则
  放行/阻断，发布与取消发布均有确认弹窗。
- 发布管理：只能勾选连续前沿内的章节；取消发布只能从最后一章依次进行。

## 18. 大纲顺序标签：幕改卷 + 卷标/章标渲染模式独立切换

### 做了什么

- `ui-store.ts`：新增 `actNumMode: OutlineNumMode`（卷标三态，默认 'cn'）与
  `cycleActNumMode`；persist partialize 增加该字段，migrate 同步处理旧值
  'hidden' → 'blank'。章标 `outlineNumMode` 不变，两者互不影响。
- `OutlinePanel.tsx` OrderTag：按 variant 取各自模式（act → actNumMode /
  node → outlineNumMode），点击只循环自身的三态；后缀「幕」→「卷」
  （类型、aria-label、tone 判定、tooltip）。空态文案与新建幕占位符同步改为
  「第一卷 / 卷标题（如：第一卷 · 雨夜来信）」；`OutlineExpandedView.tsx`
  空态文案同步改卷。
- 顺带提交 OutlinePanel 中第 16 节遗留的状态点 span 格式微调（无逻辑变化）。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。

### 验收

- 点击卷标只在 第一卷 / 1 / 无文字 间循环，不影响章标；反之亦然；
  两种模式各自持久化，刷新后保留。

## 19. 章节次序标默认数字

### 做了什么

- `ui-store.ts`：`outlineNumMode` 默认值 'cn' → 'num'（章节顺序标签默认显示
  「1」数字形态）。仅改新用户/未持久化用户的默认；已持久化的偏好不受影响。
  卷标 `actNumMode` 默认仍为 'cn'（第一卷）。OrderTag 注释同步。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。

### 验收

- 清空浏览器存储后进入大纲，章节点顺序标默认显示数字；点击仍可循环
  数字 → 无文字标 → 第一章。

## 20. 要点标题 ↔ 章节标题双向同步（修复文本缓存不同步）

### 现象

大纲要点标题改名后，章节列表 / 中央章节 tab 标签仍是旧名；反之在正文编辑器
改章节标题，大纲要点标题也不跟随。

### 做了什么

- `OutlineNodeEditor.tsx` commitNodeEdit：检测标题变更（trim 后与节点旧标题
  比较），有变更且已绑定章节时调 `renameChapter`（联动章节列表 / currentChapter，
  EditorArea 既有镜像 effect 会同步激活 tab 标题），并直接 renameTab 该章节 tab
  （覆盖非激活 tab 不被镜像 effect 覆盖的情况）。状态切换 / 发布等无标题变更
  的提交不触发重命名。
- `EditorArea.tsx` commitTitle：正文章节标题提交后，反向遍历大纲找到绑定该
  章节的要点，`updateNode({ title })` 使要点标题跟随。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0）。

### 验收

- 节点页改要点标题并保存 → 章节列表 / 中央 tab / 正文页标题全部更新；
- 正文页改章节标题 → 大纲要点标题同步更新；两侧互改无回环（草稿不受
  外部刷新打断，均在各自提交时机生效）。

## 21. 补交遗留批次：Home tab 化 + 顶栏重排 + 漏交的 cn-numeral.ts

### 背景

工作区遗留一批未提交改动（更早轮次完成后未 commit）；更严重的是
`utils/cn-numeral.ts` 从创建起一直是 untracked，而已提交的 OutlinePanel
早已 import 它——远端仓库当前无法构建。本次一并补交。

### 做了什么（按遗留 diff 归纳）

- `tab-store.ts`：TabKind 增加 `overview` / `story`（Home 类可关闭 tab）；
  `overviewTabKey(novelId)` / `STORY_TAB_KEY` 常量。
- `novel-store.ts`：恢复上次作品后重开其全书首页 Home tab；删除作品时关闭
  其首页 tab。
- `outline-store.ts`：新增 `addNodeAt`（幕内按位置插入，越界收敛）。
- `NovelOverview.tsx`：改为接收 `novelId` prop（支持多作品首页并存，不依赖
  currentNovel）；AI 起稿入口右置。
- `StoryWorkflowPanel.tsx`：接收 `tabKey`，「返回概览」改为打开首页 tab 并
  关闭自身。
- `WorksBar.tsx` / `useCommandItems.tsx`：点击作品改走 `openPanelTab(overview…)`。
- `TopBar.tsx`：布局重排——左区 Logo+AI 标+社区入口，中区搜索缩窄
  （480→360px），右区创作场景切换+最大化+用户入口。
- 新增文件 `utils/cn-numeral.ts` 补交入库（修复远端构建缺失依赖）。

### 端到端实测

- `pnpm exec tsc --noEmit` 通过（exit 0，含本批全部文件）。

### 验收

- 刷新后恢复作品自动回到其首页 tab；删除作品不留孤儿首页 tab；
  概览/起稿页可关闭、不再顶替编辑板；远端 clone 可编译。
