# 核心施工记录 · 2026-09-03

---

## AI 起稿滑动切换器：倒退切换失败修复 + 阶段快照预览/采纳

**做了什么**：修复滑动选择器倒退切换报「切换阶段失败」，并补齐「按阶段存储结果 + 切换回看预览 + 底部阶段采纳」能力（接上此前被中断的一轮施工）。

**为什么**：节点标签 `<button onClick={jumpStage}>` 与轨道 `onPointerUp={handleDragEnd}` 都会触发 `jumpStage`，单次点按/拖动 = 两次 `POST /story/jobs/:id/stage`；该端点挂在 AI 限流组（`http.go:482` `RateLimiter.Scope(middleware.ScopeAI)`，`http.go:513` 注册），第二次请求被 429 拒绝 → 前端 toast「切换阶段失败」。倒退/前进都中招，倒退更常被快速点按触发。原实现也无历史阶段回看手段（预览只读当前 `stage_payload`）。

**实现了什么**：
1. 前端去重：节点标签改为非交互 `<div>`，点击/拖动统一由轨道 pointer 事件处理（pointerdown 设 `dragStageIdx`、pointerup → `handleDragEnd` 单次 `jumpStage`），消除重复提交。
2. 后端按阶段存结果：`StoryJob` 新增 `StageSnapshots jsonb`；`GenerateStage`/`runVerifyStage` 把本阶段纯净结果写入 `stage_snapshots[stage]`（保留 `stage_payload` 存当前生成 + 采纳记录）；`toResponse` 回传 `stage_snapshots`（DTO `StoryJobResponse` 与前端 `StoryJob.stage_snapshots` 均已接）。
3. 前端切换预览：`StagePreview` 改为读 `stage_snapshots[job.stage] ?? stage_payload`，切换回历史阶段即展示当时生成结果；重新点「生成当前阶段」会覆盖该阶段快照（重做替换）。
4. 底部阶段采纳：`StagePreview` 底部新增按阶段采纳——成稿阶段「采纳到章节」（落库真实章节），其余有结果的阶段「采纳本阶段并进入下一阶段」（确认并推进）。

**取舍**：非成稿阶段的「采纳」语义等价于 `AdvanceStage`（确认结果并进入下一阶段），因大纲/分章等产物仅预览态、无独立落库端点；成稿才有 adopt→chapters 落库。`verify` 阶段为一致性报告、无采纳按钮。

**验收**：`go build ./...`（server，exit0）、`pnpm exec tsc --noEmit`（web，exit0）全绿；`/stage` 限流根因已定位并消除重复提交。

---

## AI 起稿手动页缺陷修复 + 创作意图栏补齐（C9）

**做了什么**：
1. 「AI 起稿」手动页 `StoryWorkflowPanel` 补齐创作意图栏——新增「目标受众(audience)」「创作意图(intent)」两个输入框，创建任务时随 `config` 传入（此前仅对话流配置卡 `DraftConfigCard` 已接，手动页缺失）。
2. 对话卡片 `DraftResultCard` 修正 `isVerifyJob` 定义顺序（原定义在 `applyJob` 之后且被闭包引用，易生歧义），前移到 `applyJob` 之前。
3. 对话卡片 `DraftResultCard` 修复按钮渲染逻辑不一致：「重新生成」在 `done` 阶段仍显示（与「生成下一阶段」的 `!lastStage` 隐藏不一致），补上 `!lastStage` 条件。
4. 后端 `StoryService.CreateJob` 修复阶段进度分母——`TotalSteps` 由 5 改为 7，与阶段数（idea/outline/plan_chapters/draft_chapter/verify/finalize/done）对齐，避免前端「阶段 6/5」错乱展示。

**为什么**：备忘录要求「AI起稿手动页面没做好，存在很多缺陷和逻辑错误，和创作意图栏一同施工」；后端 `formatConfig` 已支持 `audience/intent` 注入提示词，只差手动页前端接入。

**取舍与理由**：
- 创作意图栏沿用 `DraftConfigCard` 的可选文本输入（不预设枚举），placeholder 与样式保持一致。
- 章节标题仍退化为「第 N 章」（后端 `draft_chapter` 场景暂不生成 title），保持现状不做后端 title 生成，规避额外 AI 调用与风险。
- `TotalSteps` 仅对齐阶段数，`AdvanceStage`（idx+1）/`SetStage`（i）口径不变，本就等价。

**验收**：`pnpm exec tsc --noEmit`、`go build ./...`、`go vet ./...`、`go test ./internal/service/ -count=1` 全绿。

---

## AI 起稿滑动切换器：切换阶段 429 根因修复（限流作用域）

**做了什么**：
1. 后端 `internal/server/http.go`：把 story 流水线里**纯 DB 操作**的状态端点（create/list/get/delete/`/stage`/`/advance`/`/chapters/adopt）从 `aiGroup`（叠加 `ScopeAI` 1 req/s）移回 `api` 组（仅 `ScopeAPI` 20 req/s）；只有真正的 LLM 调用 `POST /story/jobs/:id/generate` 继续挂在 `ScopeAI` 上。
2. 前端 `StoryWorkflowPanel.tsx`：`handleDragEnd` 在 `jumpStage` 后**移除冗余的 `refreshActive()` GET**——`jumpStage` 已用 POST 响应更新 `activeJob`，多余 GET 既无必要，又是旧限流下被静默 429 的源头之一。

**为什么**：上一轮只去掉了节点 `<button>` 与轨道 `onPointerUp` 的双重提交（消除一次 429），但没触及真正根因——`/stage` 等状态端点和 LLM 端点共用每用户 1 req/s 的 `ScopeAI` 桶。拖动滑块来回预览会在一秒内产生多次 `POST /stage`（且旧代码每次还紧跟一次 `GET` 刷新），第 2+ 次必然 429，被 `jumpStage` 的 catch 统一 toast 成「切换阶段失败」。用户反馈「并未修复」正源于此（之前贴的 `/ws` `ECONNRESET` 是 vite 的 WebSocket 代理噪声，与 /stage REST 失败无关；429 是干净 HTTP 响应不会触发 TCP reset）。

**取舍与理由**：
- 限流作用域按"是否消耗 LLM 配额"划分：`/generate` 是真 AI 调用必须 1 req/s；`/stage`/`/advance`/adopt 只是状态机推进，20 req/s 足够且不影响体验。
- 未动 `ScopeAI` 本身（仍 1 req/s 保护 LLM 端点），仅调整路由归属。

**验收**：本地 `INKBLOOM_MODE=local` 起服务，登录演示账号后建 novel+job，连发 5 次 `POST /stage`（无间隔）全部 `HTTP 200`；对照组 `POST /generate` 仍 1 req/s 限流（第 2/3 次 `429`）。`go build ./...`、`pnpm exec tsc --noEmit` 全绿。

---

## AI 起稿阶段滑条拖动抖动修复（前端）

**做了什么**：`packages/web/src/components/story/StoryWorkflowPanel.tsx` 的阶段滑条从「离散吸附」改为「连续跟随 + 松手平滑吸附」：
- 新增 `dragRatio`（连续 0~1）状态：拖动时手柄贴着指针连续移动，`dragStageIdx` 仅用于标签高亮，消除节点边界处 `idx` 在 k/k+1 间的横跳抖动。
- 用轨道内条 `barRef` 的真实 rect 算比例，消除外层 `px-2` padding 造成的指针/手柄偏移。
- 拖动中 `left/width` 不加 transition（死贴指针）；松手后才加 `transition-[left,width] duration-200` 平滑吸附到目标节点。
- `handleDragEnd` 改在 `finally` 里才清空 `dragRatio`：手柄停在松手点等 `jumpStage` 的 POST 返回、`stageIndex` 更新后才统一吸附，去掉「先弹回旧阶段再跳新阶段」的回弹抖动。

**为什么**：旧实现 `handleTrackPointer` 用 `Math.round` 把指针吸附到离散节点，边界附近指针亚像素抖动会让 `idx` 反复横跳；手柄用 `left:%` 定位却只挂 `transition-transform`（`left` 无过渡），拖动无平滑跟随、松手也无顺滑吸附；`handleDragEnd` 立刻清空 `dragStageIdx` 导致先弹回旧阶段。

**验收**：`pnpm exec tsc --noEmit` 全绿（exit 0），vite HMR 自动生效。

---

## 全本创作窗口：书名自动取本作 + AI 填写

**做了什么**：全本创作窗口（StoryWorkflowPanel 创建表单）改为双模式。

1. 已选作品（`currentNovel` 存在）：书名直接取 `currentNovel.title`，隐藏书名输入框、显示「作品：X」只读徽标；提交 `title=currentNovel.title`，无需手填。
2. 未选作品（新建小说入口）：显示可编辑书名输入框 + 「AI填写」按钮（Sparkles）；提交时先 `createNovel → selectNovel → createJob`，即「创建作品并开启全本创作」。
3. 新建小说页（NovelList 新建对话框）新增「创建并开启全本创作」按钮：`deselectNovel()` 清空当前选定（确保进入需填书名模式）后 `dispatchEvent('inkbloom:open-story-workflow')`，RightPanel 已监听该事件切换至 story Tab。
4. `novel-store` 新增 `deselectNovel()`（清空 `currentNovel` + 移除持久化 id `inkbloom:currentNovelId`）。
5. AI 填写：新增 `suggestStoryTitle(idea)`（ai-actions-client.ts），调用预留端点 `POST /ai/story-title`（与 `/ai/candidates`、`/ai/inspiration` 同约定），DEV 环境降级为本地启发式 mock（从创意抽中文关键词套网文命名模式生成候选）；预留端点未实现时按钮在本地始终可演示。

**为什么**：需求——已选小说则全本创作窗口不必编辑书名；新建小说页调取全本创作窗口时需填书名且支持 AI 自动起名。

**取舍**：未复用 `/agent/chat` 真实 LLM 端点——它是带工具调用的 Agent 循环，仅起名可能触发 `create_novel` 等副作用；故走「预留端点 + DEV mock」的安全路径，后端接入真实 LLM 后返回 AI 书名。

**验收**：`pnpm exec tsc --noEmit` 全绿（exit 0）。改动 4 个前端文件：StoryWorkflowPanel.tsx、ai-actions-client.ts、novel-store.ts、NovelList.tsx。

---

## WS /ws ECONNRESET 修复（cloud 模式根因）

**根因**：dev 服务跑 cloud 模式（`run_mode=cloud`），`IsLocal()` 为 false；`CORSOrigins` 仅来自环境变量 `INKBLOOM_SERVER_CORS_ORIGINS`，未配置则为空 → WS 白名单恒空 → vite（localhost:3000）握手被 403 → `read ECONNRESET`。此前 gated 在 `IsLocal()` 的修复因此失效。

**修复**：`cors.go` 导出 `DefaultDevOrigins`（含 localhost:5173/3000、127.0.0.1:5173/3000、127.0.0.1:18080）；`main.go` 改为 `CORSOrigins` 为空时回退 `middleware.DefaultDevOrigins`，与 CORS 中间件同契约（cloud-dev 与 local-dev 均生效）。`websocket.go` 的 `originAllowed` 白名单因此放行 dev 源 → 101。

**取舍**：不依赖 `IsLocal()`，改为「配置为空→dev 默认源」的统一回退，避免环境差异再次漏配。

**验收**：`go build ./internal/middleware/` 与 `go build ./cmd/server/` 均 exit 0。本地 `INKBLOOM_MODE=local` 起服务后，对 `/ws` 做浏览器式握手探活（带 `Origin` + `Connection: Upgrade` + `Sec-WebSocket-Version:13` + `Sec-WebSocket-Key` 头），结果：
- `Origin: http://localhost:3000`（vite dev）→ **HTTP 101 升级成功** ✅
- `Origin: http://127.0.0.1:3000` → **101** ✅
- `Origin: http://127.0.0.1:18080`（桌面内嵌 shell）→ **101** ✅
- `Origin: http://evil.example.com`（负向）→ **403**，`websocket.go:199` 记 `WebSocket origin rejected` ✅
- 无 `Origin`（curl / 桌面 loopback 页）→ **101** ✅

即 dev 源握手成功升级、非法源被拒，根因闭环。

---

## AI 起稿迁移为中央窗口（合并创建+工作流）

**背景**：需求——AI 起稿从右侧栏常驻改为正式中央窗口，合并「创建作品 + 全本创作工作流」；书名两端均可编辑并支持 AI 笔填入；简介编辑从概览页迁入起稿窗口；概览页简介改只读；中央面板便于检查 agent 工作流，右侧栏不再需要常驻 AI 起稿。

**做了什么**：
- ui-store 新增 `centerTab: 'overview' | 'story'`（不持久化）；`inkbloom:open-story-workflow` 事件改为置 `centerTab='story'`。
- EditorArea：无打开章节且 `centerTab==='story'` 时渲染 `StoryWorkflowPanel`（不限于已选作品，新建小说入口也走此中央窗口）。
- StoryWorkflowPanel 根容器改为 `flex-1 flex items-center justify-center bg-surface-0 relative overflow-hidden` + 光晕；书名 input 两端通用可手动改 + AI 笔（PenLine）填入；新增简介 textarea（选中作品时随作品预填、改动可「保存简介」；新建时随作品入库）；创意 placeholder 改「书名或创意」；右上角「返回作品概览」。
- RightPanel：移除 `story` Tab（NOVELIST_TABS / MEDIA_TABS）、`open-story-workflow` 监听与渲染、Wand2/StoryWorkflowPanel 导入。
- NovelOverview：简介区去编辑态，改只读；移除 editing/draft/saving 状态与 handler、Check/X 导入、DESCRIPTION_MAX、updateNovel。

**取舍**：简介在「已选作品」态提供显式「保存简介」按钮（区别于新建态直随作品入库），避免刷新即丢；选中态创建任务时若简介有改动先 `updateNovel` 再 `createJob`，书名取编辑框（默认本作标题、可改）。

**验收**：`pnpm exec tsc --noEmit` 全绿（exit 0）。改动 5 个前端文件：ui-store.ts、EditorArea.tsx、StoryWorkflowPanel.tsx、RightPanel.tsx、NovelOverview.tsx。

---

## 起稿页编辑/AIGC 开关与简介页只读

**背景**：用户明确调整设计——编辑开关应放在**中央起稿页**（StoryWorkflowPanel），而非简介页（NovelOverview）；起稿页头部左侧放 AIGC 图标（表示全部作品概览由 AI 生成，此时编辑开关置灰禁用），右侧为编辑开关；编辑开启后每个输入框右侧放星光 AIGC 图标用于逐字段 AI 填写；简介页恢复纯展示。

**做了什么**：
1. **NovelOverview 恢复为纯展示**：移除上一轮加的编辑开关、`editing/editTitle/editGenre/editDescription/saving` 状态与保存/取消处理器；只读展示书名/类型/章节数/字数/创建时间/简介；保留「AI 起稿」按钮与底部删除入口。
2. **StoryWorkflowPanel 头部 redesigned**：
   - 左侧 `AIGC` 按钮（Sparkles）：开启 `aiMode` 时禁用编辑并提示「作品概览由 AI 生成」；
   - 右侧 `编辑/完成` 开关：仅在 `aiMode` 关闭时可用，开启后书名/简介/创意三个输入框进入编辑态。
3. **输入框右侧 Sparkles AI 填写图标**（仅在编辑态显示）：
   - 书名 → `suggestStoryTitle`（已存在）；
   - 简介 → `suggestStoryDescription`（已存在）；
   - 创意 → 新增 `suggestStoryLogline`（预留 `POST /ai/story-logline`，DEV 降级为本地启发式 mock）。
4. **编辑态与只读态样式隔离**：只读时使用低对比背景/边框与 `cursor-default`；编辑态恢复可聚焦样式，输入框右侧预留 `pr-10` 避免与图标重叠。
5. **切换作品状态重置**：`currentNovel.id` 变化时自动预填书名/简介、重置 `aiMode=false`，并按是否选中作品初始化 `editMode`（新建入口默认开启编辑、已有作品默认只读）。

**取舍**：
- AIGC 概览模式目前作为「锁定手动编辑」的语义开关，不主动覆盖已有内容；避免用户手动填写的内容被误覆盖。如需「一键 AI 生成全部概览」，可在后续接入真实 `/ai/story-overview` 端点后扩展。
- 创意 AI 填写走 `suggestStoryLogline` 预留端点 + DEV mock，后端未实现时本地仍可演示。
- 移除简介区独立的「保存简介」按钮；简介改动随「创建创作任务」一并写回作品。

**验收**：`pnpm exec tsc --noEmit` exit 0；`vite build --outDir dist-verify` exit 0、2098 模块；改动 3 个前端文件：NovelOverview.tsx、StoryWorkflowPanel.tsx、ai-actions-client.ts。

---

## 修复 AI 起稿重构四类错漏

**背景**：上一轮把简介编辑迁入 `StoryWorkflowPanel` 后，用户反馈「错漏百出」四项：
(1) 简介页面不见了；(2) 未按要求的编辑开关式交互（右上角「编辑」逐字段改）；
(3) AI 填写只填了书名，应填完整简介；(4) 切换书本后中央/简介页未同步到新书。

**做了什么**：
- NovelOverview 恢复为「简介页」并加编辑开关：默认只读展示书名/类型/简介；右上角「编辑」按钮切换整页为编辑态，可手动改每字段（书名 input / 类型 input / 简介 textarea），含「保存」「取消」；`currentNovel.id` 变化时自动预填各字段并退出编辑态（修复切换书本同步）。删除入口保留。
- StoryWorkflowPanel 简介区新增「AI生成简介」按钮（PenLine）：以创意/书名为种子调用新增的 `suggestStoryDescription(idea, title?, count?)`，填充完整简介正文并给出候选；保留书名的「AI填入」笔。选中作品时生成后提示「保存简介」写回。
- ai-actions-client 新增 `suggestStoryDescription`（预留端点 `POST /ai/story-description`，`resp.descriptions[]`），DEV 降级为本地启发式多句简介 mock。
- 切换书本同步：NovelList 三处 `selectNovel` 调用（列表点击 / 展开库选中 / 新建后选中）与 useCommandItems 命令面板选中作品，均随后 `setCenterTab('overview')`，落地到新书简介页；StoryWorkflowPanel 标题/简介预填 effect 随 `currentNovel.id` 切换（原已具备，确认有效）。

**取舍**：简介 AI 生成放在起稿窗口（该处持有「创意」种子）；简介页编辑态为纯手动改每字段，不重复放 AI 按钮（避免与起稿窗口职责重叠）。书名 AI 笔与简介 AI 生成分开，各司其职。

**验收**：`pnpm exec tsc --noEmit` exit 0；`vite build`（dist-verify 绕过沙箱删 dist）exit 0、2098 模块。改动 5 个前端文件：NovelOverview.tsx、StoryWorkflowPanel.tsx、ai-actions-client.ts、NovelList.tsx、useCommandItems.tsx。

## 2026-09-03 概览 AIGC 全链路真实化 + 起稿页交互重构（七项需求）

**背景**：用户提出七项需求：0 取消按钮 + AIGC 移左；1 AI 生成前覆盖警告；2 文风/受众/意图纳入概览统一管理；
3 概览页/书名修改全站同步；4 打通真实 AIGC 链路禁 mock；5 全本生成随机性 + 热门度/创新性；6 单字段生成携带全部概览上下文。

**做了什么**：
- 后端持久化（需求2/3）：`novels` 表新增 `style/audience/intent` 三列（`model/novel.go`，AutoMigrate 自动加列，C1）；
  Create/Update DTO 与 `NovelResponse` 补三字段；`UpdateNovel` 指针语义「传空串=清空、缺省=跳过」；
  `agent_context.go` 补 `novel_description` 上下文注入（800 字预算）。
- AI 端点（需求4/5/6）：ai-service 新增 `POST /api/ai/story-overview`——提示词注入随机变体编号
  （random 1–100000）+ `temperature=0.9` + 硬性要求「兼具热门度与创新性、避免套用模板」；接收概览全字段作上下文，
  要求生成字段与已有概览保持世界观/基调/受众一致；Go 侧 `AIHandler.StoryOverview` 代理
  （aiGroup 挂 `ScopeAI` 1 req/s 限流，C10）+ `StoryOverviewRequest/Response` DTO。
- 前端（需求0/1/2/3/6）：`ai-actions-client` 重写为真实链路 `generateStoryOverview(ctx, fields)`
  （删除 `suggestStoryTitle/Description/Logline` 与全部 mock，失败直接抛错由 UI toast，禁预设选项冒充）；
  `StoryWorkflowPanel` 重构——AIGC 总开关移至标题左侧（开启后逐字段出现 Sparkles，与编辑开关正交）；
  头部新增「取消」（进入编辑时快照六字段，取消回滚并退出编辑，「完成」持久化脏字段）；
  书名/简介/创意/文风/受众/意图 六字段统一为 `OverviewFieldRow` 概览字段组（受 editMode 门控，
  生成设置仅留滑条+沉淀开关，删候选 chips）；`handleAIFillField` 统一单字段生成——目标字段非空先
  `window.confirm` 覆盖警告，请求携带概览全部字段，只读态下持久化字段立即入库并经 store 同步概览页/书列表；
  `handleCreate` 先持久化含书名在内的概览脏字段再建任务；切书 effect 预填 style/audience/intent。
- e2e 揪出并修复两个真实缺陷：① `deepseek-v4-flash` 为推理模型，reasoning 可能耗尽 `max_tokens`
  致 content 为空（usage 正常但 raw=""）→ `max_tokens` 提至 4096 + 空 content 换随机变体重试一次 + 明确错误文案；
  ② Go 侧 `aiTextTimeout=60s` 双层超时（per-request context + `jsonClient` 硬顶）扛不住长推理 →
  story-overview 专用 120s context、`jsonClient` 硬顶 150s（其余端点仍 60s，行为不变）。

**端到端实测**（本地真实链路，HTTP 级，演示账号）：
- `PUT /novels/37 {style,audience,intent}` → 200 回显三字段，持久化生效。
- `POST /ai/story-overview fields=[title]` ×3 → `deepseek-v4-flash` 真实调用，prompt_tokens=406
  （证明全上下文注入），三次分别生成「夜雨刀横」「夜雨断刀」「夜雨惊鸿」（随机性生效）。
- 修复后多字段 `fields=[logline,intent]` → 3.3s 返回 2 字段，与已有概览基调/受众一致。
- `go test ./internal/service/ -count=1` ok（含新增 `novel_service_test` / `agent_context_test`）。

**验收**：`tsc --noEmit` 0；`go build ./...` 0；`py_compile`（routes/prompts/models/scenes）0；
`vite build`（dist-verify）0；live e2e 如上。改动 18 个文件：server 7（含 2 测试）+ ai-service 5（含 1 测试）+ web 4。

---

## 2026-09-03 起稿页新 UI 浏览器级 e2e 验证（九项全过）

**背景**：七项需求交付时仅有 HTTP 级验证，新 UI（AIGC 左置/取消按钮/覆盖警告/逐字段生成/保存同步）的真实浏览器交互未验证。本轮用浏览器自动化走完整用户流程并截图留证。

**做了什么**（纯验证轮，零代码改动）：agent-browser 守护进程在沙箱内无法启动（SIGTERM），改用本机 Chrome headless `--remote-debugging-port=9222` + 自写 CDP 驱动（Node 22 内置 WebSocket/fetch 直连，四原语：meta 视口 1440×900 / nav 导航 / eval 执行 JS / shot 截图），UI 内登录演示账号后逐项操作。

**端到端实测（九项全过）**：
1. 头部布局：AIGC 开关与标题同组居左（x=543 < 编辑按钮 x=796）✅
2. 概览字段组：书名/简介/创意/文风/受众/意图六字段齐全，文风已移出生成设置（生成设置仅剩双滑条+沉淀开关）✅
3. AIGC 开启 → 六字段各出 Sparkles 按钮 ✅
4. 编辑态出现「取消/完成」双按钮；改书名后点「取消」精确回滚快照值 ✅
5. 覆盖警告：对非空字段点 ✦，window.confirm 文案逐字匹配「书名」已有内容，AI 生成将覆盖现有内容，是否继续？✅
6. confirm=true → 真实 deepseek-v4-flash 生成「剑气惊澜」（第 4 个不同书名），只读态立即持久化（GET /novels/37 回读确认），左侧书列表同步显示新标题（store 同步视觉证据）✅
7. confirm=false → 零新请求、字段不变 ✅
8. 编辑文风「冷峻武侠·改」→ 完成 → 服务端 style 同步（随后已恢复「冷峻武侠」）✅
9. 截图留证 4 张：ui-02-panel-fields / ui-03-aigc-on / ui-04-edit-cancel / ui-05-final.png ✅

**验收**：九项浏览器交互验证全过；零代码改动、零数据残留（演示数据已恢复原状）。

---

## 2026-09-03（续 4）· 起稿页收尾：移除「取消」按钮 + 创意保留加固

**背景**：用户在真实使用起稿页后提三点——①「一句话创意」输入之后没有保留文本；②「编辑切换开关」现在多余，可以取消；
③「取消」按钮会在输入文本时浮现，文本录入显然已自动保存，该按钮不需要了。

**做了什么**（仅动 `packages/web/src/components/story/StoryWorkflowPanel.tsx` 一个文件）：
- 核查「编辑切换开关」：上一轮改版（工作区未提交改动）已把 `editMode` 编辑/完成开关与 AIGC 总开关整体移除，
  六概览字段改为常驻可编辑 + 防抖 800ms 自动入库；本轮对 vite 实际下发的模块做 grep，
  确认 `editMode` / `isEditing` / `编辑态` 均无残留，无需再删。
- 移除「取消」按钮：删掉 `handleDiscardChanges`、`overviewDirty` 与头部按钮渲染块，
  头部只留「返回作品概览 →」/「← 返回任务列表」两个返回入口。
- 创意（logline）保留加固：新建作品流程原 `clearDraft('new')` 会把创意一并抹掉
  （创意不入作品表，只能靠草稿），改为先 `writeDraft(String(novel.id), { logline })` 迁到新作品作用域，再清 `'new'`。

**端到端实测**（本机 Chrome headless `--remote-debugging-port=9222` + 自写 CDP 驱动，演示账号 UI 内登录）：
- 起稿页头部按钮清单：返回作品概览 → / 线索库三 chips / AIGC 全概览 / 六字段 ✦ / 创建任务 —— `取消按钮存在? false` ✅
- 输入创意「加固测试-少年负剑出山搅动江湖风云」→ localStorage `inkbloom:story-draft:37` =
  `{"logline":"加固测试-少年负剑出山搅动江湖风云"}`；点「返回作品概览」再点「AI 起稿」回来，创意值不变 ✅
- 结论：创意保留在「切页往返 / 刷新后重选作品 / 跨作品各存各的」三条路径上均成立，未复现用户描述的丢失；
  上轮草稿机制已覆盖，本轮只补了新建作品这一条漏网路径。

**验收**：`pnpm exec tsc --noEmit` exit 0；CDP 浏览器级实测两项全过。改动 1 个文件。



---

## 大纲正文入口整合旧文章 + 修复「作品库/知识图谱消失」（HMR 模块分裂）

**做了什么**：
1. 排查用户反馈「旧文章库去哪了？知识图谱去哪了？」——代码完好（LeftPanel 一直渲染 ChapterList + KnowledgePanel），真因是 vite dev server 长时间热更后模块图分裂：页面同时存在两个 novel-store 实例（`?t=1788381159127` 与 `?t=1788376904647`），中央概览订阅新实例、左栏章节列表/知识图谱订阅旧实例（currentNovel 恒空 → 双双 return null）。touch `novel-store.ts` 使模块图失效 + 刷新后恢复单实例，面板即回归。
2. 大纲正文入口整合旧文章数据：OutlinePanel 的「写正文」在要点未关联章节时，若存在尚未挂到任何大纲要点的存量章节（旧文章），先弹「挂载旧文章为正文」选择器（标题/字数/更新时间 + 挂载 hover 标），点选即关联（planned→drafting）并打开正文；也可选「新建空正文」维持原行为。无可复用章节时保持原路径不弹窗。

**为什么**：备忘录两条——旧文章库/知识图谱去向不明；旧文章数据要能进大纲正文，避免同一篇旧文被「一律新建章节」稀释成重复空章。

**验收**：`pnpm exec tsc --noEmit` exit 0；浏览器实测（演示账号，novel 剑气惊澜）：点「写正文」弹选择器正确列出两篇未挂大纲章节 → 点「夜雨孤灯 2,705 字」→ toast「已将…挂为本要点正文」、编辑器打开该章、大纲节点状态 未开始→写作中、悬浮标变「正文」。改动 1 个前端文件。
---

## 挂载功能取消 · 文章库并入大纲（章节正文统一在大纲管理）

**背景**：备忘录——「挂载功能取消 合并文章库和大纲库 对数据进行挂载绑定 统一在大纲管理章节正文 原文章章节取消 设计的Agent调用变动要跟进」。上一轮做的「挂载旧文章选择器」整体废弃，改为数据层自动绑定。

**做了什么**：

前端（packages/web）：
1. OutlinePanel 移除挂载选择器（bodyPicker / linkExistingChapter / 孤儿章节计算 / 挂载弹窗全部删除）；「写正文」恢复为：已关联→直接打开，未关联→按大纲顺序就地建空正文并关联。
2. 删除联动（正文统一在大纲管理的闭环）：删除要点时若绑定章节，确认后一并删章；删除幕时幕下所有已绑章节一并删除（带确认）。
3. LeftPanel 作品库 Tab 移除 ChapterList（原文章/章节平铺列表取消），作品库只列作品；章节正文唯一入口在大纲面板。ChapterList.tsx 整文件删除。
4. NovelOverview「新建章节」引导语改为指向大纲面板「写正文」。

后端（packages/server）：
1. NovelDocService 新增章节↔大纲绑定模块（novel_doc_service.go）：
   - `BindChapterToOutline`：按标题键（chapterTitleKey，去序数字/引号）匹配未绑定要点挂 chapter_id（planned→drafting），无匹配则末幕追加新要点（空大纲自动建「第一幕」）；幂等。
   - `MigrateBindOrphanChapters`：启动时一次性把所有小说的孤儿章节（未被任何要点引用）绑进大纲；配合 NovelRepository 新增 `ListAll`（仅迁移用，不暴露 API）。
2. 三个写入点全部接入绑定：AI 起稿采纳（StoryService.AdoptChapter）、对话 Agent create_chapter 工具（executeTool）；均为 best-effort 不阻塞主流程。
3. Agent 提示词与工具描述跟进：create_chapter 描述改为「自动挂到大纲：同名要点绑定，否则末幕追加」；systemPrompt 增补「章节正文统一在大纲中管理，规划整本书时优先 save_outline 再逐章 create_chapter + write_chapter」。

**设计取舍**：不做「大纲读取时自愈」（GetOutline 保持无副作用）——实测会使版本快照 content-hash 不确定、且破坏大纲恢复的精确性测试；改为「创建时绑定 + 启动幂等迁移」。绑定路径不走 dedupOutlineActs（mergeOutlineAct 会丢同标题节点的绑定，造成绑定循环）。

**验收**：`go build ./...` + `go test ./internal/...` 全过（含既有 restore/版本快照安全测试）；`pnpm exec tsc --noEmit` exit 0。前端净删 ~200 行。

---

## 接入智谱 GLM 模型组（Model Selector）

**背景**：用户提供智谱开放平台 API Key，要求把 glm-4.5-air 及其他按 tokens 计费的基础模型配入项目模型选择。

**做了什么**：
1. ai-service `config.py`：新增 `glm_api_key` / `glm_base_url`（默认 `https://open.bigmodel.cn/api/paas/v4`，OpenAI 兼容端点）。
2. ai-service `openai_provider.py`：按模型名路由客户端——`glm-*` 走 GLM 凭据端点，其余走默认 DeepSeek 端点；未配置 GLM key 时回落默认客户端。chat / chat_with_tools / stream 三个调用点全部接入。
3. `.env` 写入 INKBLOOM_GLM_API_KEY（gitignore 内，不入库）；`.env.example` 补占位说明。
4. 前端 `ModelSelector.tsx`：新增智谱 GLM 分组共 16 个模型（glm-4.5-air / airx / flash、glm-4.7-flash / flashx、glm-4.6、glm-4.7、glm-5.1、glm-5.2、glm-5.3、glm-4-plus、glm-4-air(-x)、glm-4-flash(-x)-250414、glm-4-long）；ModelOption 增加 `group` 字段，下拉列表按厂商插分组标题并加 max-h 滚动。

**验证**：py_compile 通过；tsc --noEmit exit 0；客户端路由单测断言通过；用免费模型 glm-4.5-flash 真实调用 bigmodel 冒烟通过（key 有效，thinking 在 reasoning_content 不污染 content）。Go 侧计费按 usage 结算与模型无关，无需改动。

---

## 旧文章库数据清理（孤儿章节删除 + 大纲绑定数据保留）

**背景**：文章库并入大纲后，存量旧文章章节未被任何大纲要点绑定，仍在推高 novels.word_count。用户要求：删除旧文章，保留大纲管理器绑定数据，字数不再统计弃用数据。

**数据勘察（开发库实况）**：
1. 大纲存在幽灵绑定：本地降级模式（后端未启动时前端降级 localStorage）用 Date.now() 生成章节 ID，大纲 PUT 同步到服务端后这些章节从未落库。用户点「写正文」时 chapters.find 落空 → 重复生成空壳章（42 号书 17 个、38 号书 8 个）。
2. 真实内容章（42 号书 79-88，20924 字；34 号书 2541+284+170 字等）标题与大纲节点标题匹配但未绑定。

**做了什么**：
1. 启动迁移从 MigrateBindOrphanChapters 改为 MigrateCleanupOrphanChapters（novel_doc_service.go），三步：①幽灵绑定修复（chapter_id 指向不存在章节的节点解除绑定，止血空壳章再生）②标题重绑（chapterTitleKey 去「第N章」序号匹配，word_count 降序优先 → 真实内容章赢过空壳章；迁移永不追加新节点，孤儿即弃用数据）③剩余孤儿软删 + RefreshNovelWordCount 重算。幂等；大纲解析失败的小说整本跳过（绝不盲删）。
2. DeleteChapter 删章后重算字数（此前 UI 删要点联动删章后字数不更新）。
3. 回归测试 novel_doc_service_test.go：断言 bug 回归（JSON 解析数据上的 chapterReferenced/bindChapterByTitle/appendChapterNode 不丢节点）、mergeOutlineAct JSON 形状回归、清理迁移集成测试（幽灵修复/标题重绑/孤儿删除/字数重算/幂等）。

**事故与恢复（重要教训）**：
- 首跑迁移把 73 个活跃章节全部软删。根因：json.Unmarshal 到 []map[string]any 后内层 nodes 是 []interface{}，`act["nodes"].([]map[string]any)` 静默失败 → 所有节点不可见 → 全部判为孤儿。新增 actNodes() 辅助函数（兼容两种形状）修复 novel_doc_service.go 4 处 + outline_normalize.go mergeOutlineAct 2 处。
- 恢复：软删除按 deleted_at 时间窗精确回滚 73 章，修复后重跑，结果核验无误。结论写进备忘录：遍历大纲节点必须走 actNodes()。

**清理结果（开发库）**：删 39 章（42 号书 17 空壳、38 号书 8 空壳+2 旧文章、2/18/19/32 号书全部旧文章、33 号书 2 个无匹配重稿、34 号书 2 个无匹配、37 号书 1 个）。保留：42 号书 10 章全绑（20924 字）、34 号书 18 章（2995 字）、33 号书 1 章（3860 字）、37 号书 1 章（2705 字）、38 号书 4 空壳绑回大纲。全部绑定经 SQL 核验指向真实章节 ID。

**验收**：go build + go test ./internal/... 全过（含新增回归）。

---

## 模型切换链路修复 + 默认模型切 GLM

**背景**：用户反馈模型调用没有按要求切换。排查发现主聊天 Agent 链路完全没传 model：前端 agentChat 不带 model、Go AgentChatRequest 无该字段、agentSvc.Run/callLLM 不传 —— 选择器只对 candidates（候选起稿）生效，对主聊天/Agent 工具循环形同虚设。

**做了什么**：
1. 实测 key 可用模型：GET open.bigmodel.cn/api/paas/v4/models 返回 10 个（glm-4.5 / 4.5-air / 4.6 / 4.7 / 5 / 5-turbo / 5.1 / 5.2 / 5.3 / 5.3-flash）。之前配的 glm-4.5-flash、airx、4.7-flash/flashx、glm-4-long、glm-4-plus 等均不在可用列表。
2. 全链透传：agent-chat-client.ts 加 model 参数 → ai-store.sendMessage 传 currentModel → Go dto.AgentChatRequest.Model → AgentService.Run(ctx,…, model) → callLLM body.model → ai-service /api/agent/chat（AgentChatHTTPRequest.model 已支持，`request.model or default`）→ OpenAIProvider 按 glm-* 前缀路由智谱端点。
3. ModelSelector 重写：去分组，扁平「模型名 + 简介」（GLM 10 个实测可用 + DeepSeek/OpenAI 5 个原有），glm-5.3-flash 置顶为默认。
4. 项目默认模型改为 glm-5.3-flash：ai-store 初始值、ai-service config.py default_model、.env INKBLOOM_DEFAULT_MODEL 三处。glm-5.3-flash 为 key 可用列表中最快最省档（冒烟通过；注意其为强制思考模型，不支持 thinking:disabled，思考 tokens 计入输出）。
5. candidates 链路原本已透传（CandidatesRequest.Model ✓），未动。

**验收**：go build/vet/test 全过；tsc --noEmit exit 0；glm-5.3-flash 真实调用冒烟通过。重启 ai-service 与 Go server 后生效。

---

## 垃圾桶机制：删除要点连章节+节点+正文进桶，恢复可重选幕间归属

**需求**（备忘录 2026-09-03）：增设垃圾桶，删除章节连同节点和正文一起进垃圾桶；回收时可以重选幕间归属回归大纲。

**后端（packages/server）**：
1. 新表 `chapter_trash`（model/chapter_trash.go，AutoMigrate 注册）：自包含快照——节点完整 JSON（node_json）、章节元信息（chapter_id/title/word_count）、正文全文快照、原幕 id/标题。正文冗余入桶是有意的：桶记录不依赖 chapters 软删行的存活，未来清理任务/同步导入不会破坏回收能力。
2. TrashService（trash_service.go），全部单事务保证大纲文档/chapters/回收记录三者一致（否则又会造出幽灵绑定）：
   - `TrashNode(novelID, actID, nodeID)`：校验作品归属 → 大纲解析定位节点并摘除（acts 写回 + version+1）→ 绑定章节 GORM 软删 → 快照入桶。纯规划节点（无正文）同样进桶，恢复逻辑统一。
   - `Restore(trashID, targetActID)`：节点插回用户重选的目标幕（targetActID 空 → 末尾新建幕「恢复的章节」）；章节复活（position 被活跃章节占用时回退到末尾，规避 (novel_id,position) 部分唯一索引冲突；行若被物理清理则用快照按原 ID 重建）；正文以快照回填；出桶。
   - `Purge(trashID)`：章节行物理删除 + 记录出桶（不可恢复的出口）。
   - `List(novelID)`：轻量列表视图（新→旧，limit 200）。
3. 路由（server/http.go，Trash handler Optional 挂载）：GET/POST `/novels/:id/trash`、POST `/novels/:id/trash/:trashId/restore`、DELETE `/novels/:id/trash/:trashId`。

**前端（packages/web）**：
1. trash-client.ts + trash-store.ts（byNovel 列表，restore/purge 按钮级 loading）。
2. OutlinePanel：删除要点/删除幕改走垃圾桶接口（原 deleteChapter+removeNode 双写路径废弃）；成功后 loadOutline + fetchChapters 对齐服务端状态（version 已 bump，本地不能直接 PUT，否则触发版本检查冲突）。头部进度条旁新增回收站入口（Trash2）。
3. 新组件 TrashModal：列表展示要点名/原幕/章节/字数/时间；「恢复」展开目标幕选择（现有幕下拉 + 新建幕默认项）→ 确认后恢复并刷新大纲与章节列表；「彻底删除」带确认。

**踩坑**：GORM 软删行上的 `Updates` 自动附加 `deleted_at IS NULL`，复活更新会静默落空（0 行更新不报错）——恢复必须 `Unscoped()`。已有集成测试覆盖（trash_service_test.go：进桶/列表/重选幕恢复/position 冲突回退/彻底删除全链）。

**验收**：go build + go test ./internal/... 全过（含新增集成测试）；tsc --noEmit exit 0。

---

## 模型选择持久化 + 场景级模型配置 + 默认模型切 glm-4.5-air

**需求**：①模型选择持久化 ②针对不同场景使用不同 AI 的模型配置 ③开发阶段统一 glm-4.5-air ④重启 ai/go 服务。

**做了什么（前端）**：
1. ai-store：currentModel 持久化（localStorage `inkbloom-ai-model`，随 setModel 写入）；新增 sceneModels 场景覆盖（`inkbloom-ai-scene-models`）+ `resolveSceneModel(scene)`（场景覆盖优先，未设置跟随全局）+ setSceneModel（空串=清除覆盖）。默认模型 glm-4.5-air（DEFAULT_AI_MODEL）。
2. 场景定义 5 个：agent（主聊天/创作Agent）、candidates（候选起稿）、inline（编辑器续写）、rewrite（改写润色）、expand（大纲扩写成稿）。全部接入 resolveSceneModel。
3. sse-client streamInline/streamRewrite 加 model 参数；outline-client expandOutlineToDraft 加 model 参数；candidates-store 改走 resolveSceneModel。
4. ModelSelector：glm-4.5-air 置顶标注「默认 · 开发统一」；弹层底部新增「场景模型配置」入口 → SceneModelSettings 弹窗（每场景下拉：跟随全局 / 全部模型）。

**做了什么（后端）**：
1. Go：dto.InlineRequest / RewriteRequest / ExpandOutlineRequest 加 Model 字段；Inline/Rewrite 转发 body 携带 model；ExpandOutline 走 proxyBound 整体透传。
2. ai-service：ExpandOutlineRequest 加 model 字段，ai_expand_outline 的 call_llm 传 request.model（inline/rewrite/agent 端点此前已支持）。
3. 默认模型：config.py default_model 与 .env INKBLOOM_DEFAULT_MODEL 均改 glm-4.5-air。

**模型解析优先级**：场景覆盖 > 前端全局选择 > ai-service default（glm-4.5-air）。后端内部服务（AI起稿采纳、知识抽取等）走 ai-service 默认。

**验收**：go build + go test 全过；tsc --noEmit exit 0。

**服务重启与运行时验证（补充）**：
- 重启后发现 glm 默认模型请求仍打到 DeepSeek 端点：openai_provider.py 的 chat/chat_with_tools 两处调用点 `self._client` 旧写法在文件中被还原（仅 stream 一处保留 _client_for），导致非流式调用全部未路由。已补齐 3 处 `_client_for(model)` 并重启 ai-service。
- 运行验证：/api/chat/complete 默认模型（glm-4.5-air）调用成功；Go /health ok（database ok）；ai /health ok。
- 注意：Go server 进程级注入了新的 INKBLOOM_JWT_SECRET（原 secret 在用户 shell 中无法复取），**已登录会话需重新登录**。后续应把 JWT secret 固化到本地启动脚本，避免每次重启失效。
