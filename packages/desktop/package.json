{
  "name": "@inkbloom/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "InkBloom desktop shell: Electron + embedded Go server (local mode) + web UI",
  "main": "dist-electron/main.js",
  "scripts": {
    "typecheck": "tsc -p tsconfig.electron.json --noEmit",
    "compile": "tsc -p tsconfig.electron.json",
    "build:server": "cd ../server && go build -o bin/server.exe ./cmd/server",
    "build:web": "pnpm --filter @inkbloom/web build",
    "start": "npm run build:server && npm run build:web && npm run compile && electron .",
    "start:fast": "npm run compile && electron .",
    "dist": "npm run build:server && npm run build:web && npm run compile && electron-builder"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^34.0.0",
    "electron-builder": "^26.15.3",
    "typescript": "~5.7.0"
  }
}
